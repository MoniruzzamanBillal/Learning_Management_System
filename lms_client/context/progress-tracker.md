# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

Live / in production (`devmats.vercel.app`) — ongoing maintenance & feature iteration, not initial build-out. The app was recently migrated to Next.js (see Recent Activity).

## Spec Implementation Status

Tracks work items defined in `context/specs/`. Update the moment implementation starts or finishes on a spec.

| Spec                                                                                                           | Status      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`01-my-courses-list-redesign.md`](specs/01-my-courses-list-redesign.md)                                       | ✅ Complete | `MyCourses.tsx`, `MyCourseCard.tsx`, `MyCourseCardSkeleton.tsx` redesigned to match `/courses`' grid/card pattern; lint + build clean; visual pass confirmed by the user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [`02-my-course-detail-redesign.md`](specs/02-my-course-detail-redesign.md)                                     | ✅ Complete | `EnrollCourseDetail.tsx`, `NoVideoPlaceholder.tsx`, `VideoLoadingSkeleton.tsx`, `ModuleShowData.tsx`, `EnrolledCourseDetailSkeleton.tsx` redesigned (header, progress bar, card surfaces, grid layout, restyled module status colors, persistent completed badge); lint + build clean; visual pass confirmed by the user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| [`03-certificate-pdf-design.md`](specs/03-certificate-pdf-design.md)                                           | ✅ Complete | `CertificateDownloadButton.tsx` redrawn as a fully vector jsPDF landscape layout (border, wordmark, title, centered name, course name/category, date, certificate ID, signature line) — no more `addImage("/certificate.jpg", ...)`, which was silently failing since that file never existed in `public/`. `CertificateTableColumn.tsx` now passes `category`/`completedOn`/`userId`/`courseId` in addition to the existing `userName`/`courseName`. Lint + build clean; manual browser verification (actually clicking "Download") left for the user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| [`04-ai-review-summary-ui.md`](specs/04-ai-review-summary-ui.md)                                               | ✅ Complete | Created `types/ai.types.ts`, `components/main/publicPage/courseDetail/AiReviewSummary.tsx`, updated `CourseDetailPage.tsx` to render AI summary above review list. Build passes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [`05-ai-course-advisor-ui.md`](specs/05-ai-course-advisor-ui.md)                                               | ✅ Complete | Created `components/main/publicPage/course/AiCourseAdvisor.tsx`, extended `types/ai.types.ts` with `TCourseAdvisorResponse`/`TCourseAdvisorRecommendation`, integrated into `CoursePage.tsx` above course grid. Build passes; lint clean in new files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [`06-ai-study-assistant-ui.md`](specs/06-ai-study-assistant-ui.md)                                             | ✅ Complete | Created `components/main/publicPage/MyCourses/EnrolledCourseDetail/AiStudyAssistant.tsx` (floating trigger + chat panel, local `messages` state, `usePost` to `POST /ai/study-assistant/:courseId`), extended `types/ai.types.ts` with `TChatMessage`/`TStudyAssistantResponse`, mounted `<AiStudyAssistant courseId={id} />` as a sibling in `EnrollCourseDetail.tsx`. Lint clean in new/changed files (pre-existing baseline errors untouched); build passes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [`08-course-catalog-filters.md`](specs/08-course-catalog-filters.md)                                           | ✅ Complete | Sort `Select` + `PriceFilter.tsx` added to `CoursePage.tsx`, wired into the existing `useFetchData` query string/`queryKey` (local `useState`, not the dead `filterSlice`). Lint + build clean. Backend companion work has since landed — `lms_server/context/specs/18-course-catalog-sort-price-backend.md` rewrote `getAllCourses` as an aggregation pipeline — so all four "Verify When Done" checkboxes are now ticked for real (live-browser-verified sort reordering and price narrowing against real catalog data), no longer blocked.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [`15-error-logs-admin-page.md`](specs/15-error-logs-admin-page.md)                                             | ✅ Complete | New `/dashboard/admin/error-logs` page (list + detail modal), following the exact `ManageInstructorPage.tsx` pattern, consuming `lms_server`'s admin-only `GET /api/error-log`/`GET /api/error-log/:id`. Lint + build clean; Playwright-verified against the live local backend (real error rows, status badges, detail modal with stack trace); 401 confirmed for no-token/non-admin access.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| [`09-admin-dashboard-charts.md`](specs/09-admin-dashboard-charts.md)                                           | ✅ Complete | `recharts` added; `RevenueChart.tsx`/`EnrollmentChart.tsx`/`ChartSkeleton.tsx` added under `Stat/`, consuming the extended `lms_server` `admin-stats` response. `StatPage.tsx`'s hardcoded `"78%"` replaced with the real `averageCompletion` value. Lint + build clean; Playwright-verified real chart rendering including empty-state handling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| [`10-review-moderation-ui.md`](specs/10-review-moderation-ui.md)                                               | ✅ Complete | New `/dashboard/admin/manage-reviews` page (`ManageReviewPage.tsx`), following the `ErrorLogsPage.tsx` pattern, consuming a new `lms_server` admin-only `GET /review/all` (added as a small, user-approved extension of backend spec `13-review-moderation-api.md`, which only covered delete) and `DELETE /review/:reviewId`. Lint + build clean; Playwright-verified end-to-end including non-admin redirect.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [`17-postgres-id-migration.md`](specs/17-postgres-id-migration.md)                                             | ✅ Complete | `_id`→`id` compiler-driven sweep (102 occurrences/43 files) plus a follow-on FK-field-name fix the original plan didn't anticipate. `yarn build`/`yarn lint` clean at the established baseline; Playwright-verified end-to-end against the live migrated backend. Found and fixed 2 backend regressions along the way (see `lms_server`'s progress tracker).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [`16-frontend-folder-structure-migration.md`](specs/16-frontend-folder-structure-migration.md)                 | ✅ Complete | Reorganized `lms_client`'s folder structure to match a reference project's conventions: `components/Dashboard/` split into `components/dashboard/sidebar/` (chrome only) and `components/main/(Admin\|Instructor\|User)/<Feature>/` (all dashboard feature screens, colocated with their own `column/`/`schema/`/`type/`/`functions/` subfolders); `components/main/publicPage/` flattened directly into `components/main/`; `components/input/` merged into `components/shared/input/`; `services/`/`actions/`/top-level `schemas/` removed (merged into `lib/` or colocated per-feature); `utils/axiosInstance.ts`/`api.ts` moved to `lib/`; new `providers/` folder for `QueryProvider.tsx`/`StoreProvider.tsx`; single-feature `types/*.types.ts` files colocated, cross-feature ones (`course`, `user`, `auth`, `ai`, `globalTypes`, `module`) stayed global. ~135 files moved via `git mv`, ~104 import paths fixed (84 `@/`-alias + ~20 relative). Pure file-move + import-path-fix — no component logic changed. `yarn lint`/`yarn build` both clean (lint's 31 pre-existing errors are unrelated code-quality issues, not import breaks). `CLAUDE.md`, `architecture.md`, `ui-context.md`, `code-standards.md`, and `ai-workflow-rules.md` updated to the new paths. Manual browser click-through of all three dashboards left for the user — see spec 16.                                                                                                                                                                                                                                                                                                                                                                                                           |
| [`19-error-message-passthrough-fix.md`](specs/19-error-message-passthrough-fix.md)                             | ✅ Complete | 12 call sites across 5 `*.functions.ts` files (`course.functions.ts` x3, `module.function.ts` x2, `video.functions.ts` x3, `auth.functions.ts` x1, `profile.functions.ts` x1) read the dead pre-interceptor Axios shape `error?.response?.data?.message` — always `undefined` on what `lib/axiosInstance.ts`'s response interceptor actually rejects with (a flattened `{statusCode, message, errorMessages, errors}`, no `.response` key) — so every specific backend error toast silently fell back to a generic string. Fixed by changing all 12 to `error?.message`. Found while seeding course data (`lms_server` spec 21) and re-testing a duplicate-instructor-email submission. Live-verified via Playwright (toast text now shows the real backend message); `yarn lint` no new issues.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [`20-file-input-onchange-override-bug.md`](specs/20-file-input-onchange-override-bug.md)                       | ✅ Complete | **Critical bug, found while seeding course data (`lms_server` spec 21).** 6 file `<input type="file">` fields across the app (`AddVideo.tsx`, `UpdateVideo.tsx`, `AddCourse.tsx`, `UpdateCourse.tsx`, `AddInstructor.tsx`, `UpdateProfile.tsx`) spread `{...register(fieldName)}` and then declared a separate `onChange={...}` prop right after it purely to drive a local preview — the later JSX prop silently replaced RHF's own `onChange`, so the selected file never reached react-hook-form's internal state. For `AddVideo.tsx` (required field) this meant **every real "Add Video" submission 500'd** (`videoUrl` missing) no matter what the user picked; for the other 5 (all optional fields) it silently dropped cover images/profile pictures with no error. Fixed identically in all 6: capture `register(...)`'s result in a local variable, then call its real `.onChange`/`.ref` from inside the wrapping handler. Live-verified via Playwright: a real 22MB video upload that previously sent an empty body now uploads for real (~20s, real Cloudinary `videoUrl`); this fix directly unblocked the course-seeding run. `yarn lint` no new issues in any of the 6 touched files.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| [`18-update-profile-page.md`](specs/18-update-profile-page.md)                                                 | ✅ Complete | New `components/main/Profile/UpdateProfile.tsx` (name + avatar edit form) at `app/dashboard/update-profile/[userId]/page.tsx`, filling the previously-dead link on `ProfilePage.tsx`. Consumes `PATCH /user/update-user`, unchanged except for a new `updateUserValidationSchema` (`user.validation.ts`) wired via `validateRequest` (`user.route.ts`) — that route had zero server-side validation before. Deviated from the spec text in two small, justified ways: used `usePatch` (matches the route's actual `PATCH` method) rather than the spec's suggested reuse of the dead `imageSchema` for the image field — `z.any().optional()` was used instead, matching `AddCourse.tsx`/`UpdateCourse.tsx`'s established pattern, since `imageSchema`'s `string \| File` union doesn't type-check against a file input's `FileList` (`data.image?.[0]`); and avoided the `set-state-in-effect` lint error `UpdateCourse.tsx` already carries by deriving the avatar preview (`newImagePreview ?? userData?.profilePicture`) instead of syncing fetched data into local state via `setState` inside `useEffect`. `yarn lint`: 31 errors (unchanged baseline) + 1 new warning (`<img>`, same accepted class as `UpdateCourse.tsx`'s identical warning — blob-URL preview isn't a good fit for `next/image`). `yarn build` clean in both apps; new route generated. Live-verified against the real local backend: `PATCH /update-user` returns a real Zod 400 for an empty name, and a real 401 once validation passes but no token is sent — confirming the new `validateRequest` wiring runs in the correct order ahead of `authCheck`. Browser click-through (submitting the form as a logged-in user) left for the user, per this environment's no-real-session constraint. |
| [`21-signup-registration-payload-format-bug.md`](specs/21-signup-registration-payload-format-bug.md)           | ✅ Complete | **Critical, found doing a real-user E2E smoke test** (sign-up → purchase → watch → certificate → review). `SignUpPage.tsx` sent its payload as a plain JS object; `/auth/register` expects `FormData` with a `data` field (`JSON.parse(req.body?.data)`, same contract `AddInstructor.tsx` already follows for `/register-instructor`) — every public sign-up attempt, ever, 500'd with `"undefined" is not valid JSON`. Fixed by wrapping the payload in `FormData` before posting, matching the existing precedent exactly. Extracted the form out of `app/(main)/sign-up/page.tsx` into `components/main/SignUp/SignUpPage.tsx` (it was `"use client"` inline in the page file, blocking a `metadata` export — done alongside the site-wide per-page-metadata pass earlier this session). Live-verified via curl (`-F 'data=...'` → 200) and Playwright (real sign-up + login succeeded).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| [`22-course-detail-enrollment-review-check-404.md`](specs/22-course-detail-enrollment-review-check-404.md)     | ✅ Complete | Found in the same E2E smoke test. `CourseDetailPage.tsx` called `/enroll/check-user-enrolled/${id}` and `/review/check-review-eligibility/${id}` as if `courseId` were a route param and never sent `userId` — both backend routes actually read `courseId`/`userId` from `req.query`, so neither route existed and both always 404'd (visible as the course-detail page sitting on its skeleton loader for ~7-10s while TanStack Query retried, then permanently showing "Enroll Now" for already-enrolled users and never showing the review form for eligible ones). Fixed by sending both as query strings, `userId` included (already in scope via `userData?.userId`). Live-verified via curl and Playwright — no more 404s, instant load, "✓ Course Enrolled" and the review form both now render correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| [`23-review-submit-missing-feedback.md`](specs/23-review-submit-missing-feedback.md)                           | ✅ Complete | Found in the same E2E smoke test. `CourseDetailPage.tsx::handleAddReview` posted the review successfully (DB row created, list/eligibility silently refetched via `usePost`'s query invalidation) but gave the user zero feedback either way — the success branch was a stray `console.log`, no `try/catch`. Fixed: `toast.success`/`toast.error` added, form state (`review`/`rating`) reset on success, dead commented-out refetch block and both `console.log`s removed. `yarn lint` clean on the touched file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| [`24-update-course-instructor-preselect-bug.md`](specs/24-update-course-instructor-preselect-bug.md)           | ✅ Complete | User report. `UpdateCourse.tsx` fetched with `useFetchData<TCourseData>` (defaults `instructors` to `string[]`) but `admin-course-detail` returns populated instructor objects, so `reset()` fed the `react-select` `[{id,name,...}]` instead of `[id]` — `.includes(id)` could never match, so no instructor ever showed pre-selected. Fixed by typing the fetch `TCourseData<TInstructor>` and mapping `course.instructors` to `.map(i => i.id)` before `reset()`. Live-verified — "Instructor Two" now pre-selects correctly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| [`25-manage-modules-grouped-by-course.md`](specs/25-manage-modules-grouped-by-course.md)                       | ✅ Complete | User-requested redesign: `/dashboard/admin/manage-modules`'s flat table repeated a course's name once per module (6 modules = 6 repeated rows), and its "Videos" column had been commented out earlier since `lms_server` spec 26's bug made it always show `0`. Replaced the flat `GenericTableComponent` table with a new reusable `components/shared/table/ModulesByCourseAccordion.tsx` — one collapsible `Accordion` section per course (name, published badge, module count in the trigger; `type="multiple"` so several courses can stay open at once), each expanding to that course's modules with real video counts (now populated correctly) and a `TableRowActions`-rendered action slot passed in by the page. `ManageModule.tsx` rewired to use it; the now-fully-unused `ManageModuleColumns.tsx` (Admin) deleted (confirmed no other importers via grep). Live-verified via Playwright: 5 courses each render once, multi-expand works, real per-module video counts, "View Details" navigates correctly. `tsc --noEmit`/`yarn lint` clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [`26-instructor-manage-modules-grouped-by-course.md`](specs/26-instructor-manage-modules-grouped-by-course.md) | ✅ Complete | Follow-on to spec 25 — same treatment for `/dashboard/instructor/manage-module`, which hits the identical `/module/all-module` endpoint with the same repeated-course-name problem. Reused `ModulesByCourseAccordion` unchanged (it was already generic via its `renderActions` prop), passing the instructor's 3 actions (View Details / Update Module / Add New Video, both conditional ones kept `hidden: course.published`) instead of the admin's 1. `ManageModuleColumns.tsx` (Instructor) deleted (no other importers). "Add Module" button above the accordion kept as-is. Live-verified via Playwright: published course's modules show only "View Details"; unpublished course's modules show all 3 actions with correct video counts (4/4/4/3); "Add Module" still navigates correctly. `tsc --noEmit`/`yarn lint` clean.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| [`28-video-notes-ui.md`](specs/28-video-notes-ui.md)                                                           | ✅ Complete | New `VideoNotesPanel.tsx` (enrolled-course player page, below the video) lets a student view/write/edit/delete their personal per-video note, consuming `lms_server`'s `video-note` endpoints. `tsc --noEmit`/`yarn lint`/`yarn build` clean; backend round-trip (empty→create→edit→delete→reactivate) curl-verified against a local dev server with a locally-signed test-only JWT. Actual browser click-through not performed — no browser-automation tool was available in this session; left for the user.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| [`29-module-quiz-ui.md`](specs/29-module-quiz-ui.md)                                                           | ✅ Complete | Student `QuizPanel.tsx` (question mode / permanent no-retake results mode) mounted in `EnrollCourseDetail.tsx`, triggered by a new "Quiz" row in `ModuleShowData.tsx`'s per-module accordion (shown only when `module.quiz` is non-null). Instructor authoring: new `components/main/(Instructor)/ManageQuiz/` (`ManageQuiz.tsx`/`QuizForm.tsx`/`QuizQuestionFields.tsx`, `react-hook-form` `useFieldArray` nested for questions→options — first use of `useFieldArray` in this codebase), reached via a new "Manage Quiz" row action on `ManageModule.tsx` (Instructor). `tsc --noEmit`/`yarn lint` clean (zero issues in any new/touched file). Full Playwright click-through against the real local stack (both dev servers, live Neon DB): instructor create (2 dynamic questions, correct-answer radios) → redirect → re-open pre-filled edit form → student answers both questions → correct score + red/green coloring → page reload → same permanent results (no retake form ever reappears) → delete via the real UI → module's "Quiz" row disappears for students; a never-quizzed module correctly shows no row at all. Surfaced 2 real backend bugs during this testing, both fixed the same session — see `lms_server` spec [`30-quiz-recreate-after-delete-crash.md`](../../lms_server/context/specs/30-quiz-recreate-after-delete-crash.md).                                                                                                                                                                                                                                                                                                                                                                                                                   |
| [`27-update-module-ownership-guard.md`](specs/27-update-module-ownership-guard.md)                             | ✅ Complete | Frontend follow-on to `lms_server` spec 27 (real bug: `PATCH /update-module/:id` had no ownership check at all — any instructor could edit any module). `UpdateModule.tsx`'s empty course dropdown, reported as a bug, turned out to be the _correct_ symptom of the logged-in instructor genuinely not being assigned to that module's course — the real fix was closing the backend authorization gap, not the dropdown logic (which was already correct). Added a proactive `isOwner` check (`moduleData.instructorId === userInfo.userId`) that renders a clear "you don't have access to update this module" state with a link back to Manage Modules, instead of a confusing blank form, once the backend enforces the real 403. Live-verified: the owning instructor still sees the normal pre-selected form; a non-owning instructor sees the new guarded state. `tsc --noEmit` clean; `yarn lint` shows only 2 pre-existing errors on untouched lines.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| [`30-assignment-ui.md`](specs/30-assignment-ui.md)                                                             | ✅ Complete | Feature 3 of 3 from the notes/quiz/assignment plan (Notes and Quiz both shipped — specs 28/29). Consumes `lms_server` spec `31-assignment.md`'s new `assignment` endpoints. Mirrors the shipped Quiz UI patterns throughout: student `AssignmentPanel.tsx` (locked once graded, editable otherwise) in `EnrollCourseDetail.tsx`, triggered by a new "Assignment" row in `ModuleShowData.tsx`; instructor authoring via a new `components/main/(Instructor)/ManageAssignment/` reached from a "Manage Assignment" row action on `ManageModule.tsx`, plus a new `SubmissionsTable.tsx` grading view (no Quiz analog — assignments are manually graded). Not yet implemented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| [`31-assign-course-detail-manage-links.md`](specs/31-assign-course-detail-manage-links.md) | ✅ Complete | User report: instructor had no way to reach assignment grading from `/dashboard/instructor/assign-course-detail/[courseId]` — the grading UI (spec 30) was already fully built and working, but its only entry point was a "Manage Assignment" row action on the *different* `ManageModule.tsx` page. Same gap existed for "Manage Quiz". Fixed by adding the same two `TableRowActions` entries (`ClipboardList`/`HelpCircle` icons, `hidden: isPublished`) already proven on `ManageModule.tsx` to `AssignCourseDetailColmn.tsx` — the only file touched. No backend or type changes. `npx tsc --noEmit` clean; `yarn lint` unchanged at the established 28-error/17-warning baseline (zero issues in the touched file); `yarn build` clean, all 31 routes generated. Manual browser click-through left for the user. |
| [`32-manage-assignment-hidden-after-publish.md`](specs/32-manage-assignment-hidden-after-publish.md) | ✅ Complete | User report ("no system to mark assignments from the instructor side") — turned out grading was already fully implemented (spec 30), but its only two nav links (`ManageModule.tsx`, `AssignCourseDetailColmn.tsx`) carried `hidden: isPublished`, copied from the structural-edit actions ("Update Module"/"Add New Video"/"Manage Quiz"). Since assignment submission requires the course be published, the entry point to grading disappeared exactly when there were real submissions. Confirmed "Manage Quiz" has no results view (quiz is content-edit-only, correctly stays hidden post-publish) — only "Manage Assignment" was wrong. Fixed by dropping `hidden: isPublished` from the "Manage Assignment" action in both files; all other actions unchanged. No backend changes — `ManageAssignment.tsx` already allowed edit/delete regardless of publish state, so this only makes an existing page reachable. `yarn lint`: zero issues in either touched file. Superseded by spec 33's page split, which is the more correct long-term fix. |
| [`33-split-assignment-manage-and-grade-pages.md`](specs/33-split-assignment-manage-and-grade-pages.md) | ✅ Complete | Follow-on user feedback: create/edit and grading shouldn't share one component/page. Split `ManageAssignment.tsx` (form + delete only now, plus a "View & grade submissions →" link once an assignment exists) from a new `GradeAssignment.tsx` (fetches the assignment then renders the unchanged `SubmissionsTable.tsx`; empty state + link to create when no assignment exists yet), new route `/dashboard/instructor/grade-assignment/[moduleId]`. Nav: added an always-visible "Grade Assignment" (`ClipboardCheck`) row action in `ManageModule.tsx`/`AssignCourseDetailColmn.tsx`, and restored `hidden: isPublished` on "Manage Assignment" now that it's edit-only — matching "Update Module"/"Add New Video"/"Manage Quiz"'s post-publish content lock, while grading stays reachable regardless of publish state (the actual fix spec 32 needed). No backend changes. `npx tsc --noEmit` clean; `yarn lint` unchanged at 28 errors/17 warnings baseline, zero issues in any new/touched file; `yarn build` clean, new route generated. Manual browser click-through left for the user. |

## Completed (already implemented)

- Public site: home, course catalog + detail, about-us, contact, faqs, instructors, login/sign-up, SSLCOMMERZ payment-outcome pages.
- Auth: login, registration, JWT-cookie-based session, role-gated routing via `middleware.ts`.
- Admin dashboard: course CRUD + publish, instructor CRUD, module management, enrollment management, admin home/stats.
- Instructor dashboard: assigned-course view, module CRUD, video CRUD.
- User dashboard: enrolled courses, certificates.
- Shared: profile page, change-password.
- Data layer: `hooks/useApi.ts` (TanStack Query) + `functions/*.functions.ts` orchestration pattern in active use across course/module/video/review/enrollment flows.
- Admin dashboard: real revenue/enrollment charts + real average-completion stat (`Stat/StatPage.tsx` + `RevenueChart.tsx`/`EnrollmentChart.tsx`, via `recharts`), review moderation (`ManageReview/ManageReviewPage.tsx`, list + delete).

## Recent Activity (this session)

- Implemented [`30-assignment-ui.md`](specs/30-assignment-ui.md): Feature 3 of 3 from the notes/quiz/assignment plan (Notes/Quiz shipped — specs 28/29), consuming `lms_server` spec `31-assignment.md`'s `assignment` endpoints. **Student side:** `EnrollCourseDetail.tsx` gained `activeAssignment: {moduleId} | null` state, mutually exclusive with `activeQuiz` (selecting either clears the other; selecting a video clears both). `ModuleShowData.tsx`'s accordion gets an "Assignment" row (`ClipboardList` icon) after a module's videos/quiz row, gated on the new `module.assignment: {id}|null` field (added to `TModule`; the backend select was already extended in the `lms_server` session that shipped spec 31). New `AssignmentPanel.tsx` fetches `/assignment/take/:courseId/:moduleId`, renders `instructions` via `dangerouslySetInnerHTML` (any embedded link renders as a real clickable `<a>`), and uses an uncontrolled `Textarea` (`key={assignmentId}` + `ref`, mirroring `VideoNotesPanel.tsx`'s established pattern) rather than a state-synced-in-effect textarea — deliberately avoiding the `set-state-in-effect` lint error that pattern would otherwise trip (confirmed by first writing the naive controlled-state version, hitting the lint error, then rewriting to match the proven ref-based pattern). Locked (graded) state disables the textarea and shows score/feedback instead of a submit button; a submit against an already-graded assignment (stale tab) refetches to flip the panel to locked state rather than staying stuck. **Instructor side:** new `components/main/(Instructor)/ManageAssignment/` (`ManageAssignment.tsx`, `AssignmentForm.tsx` — `react-hook-form` + `TextEditorTipTap` via `Controller` + a plain native `<input type="date">` for `dueDate`, submitted as bare `"YYYY-MM-DD"` per the backend's `z.coerce.date()` — `SubmissionsTable.tsx`, new, no Quiz analog since assignments are manually graded: one row per student, `content` rendered as a clickable link when URL-like via a simple regex, a status badge, an inline "Grade" form (score 0–10 + feedback, client-validated before the network call) or a read-only graded view + "Reopen" button). New `app/dashboard/instructor/manage-assignment/[moduleId]/page.tsx` and a "Manage Assignment" row action on `ManageModule.tsx` (Instructor), hidden once published — same restriction as "Manage Quiz". New top-level `functions/assignment.functions.ts` (`submitAssignmentFunction`/`createAssignmentFunction`/`updateAssignmentFunction`/`deleteAssignmentFunction`/`gradeSubmissionFunction`/`reopenSubmissionFunction`), mirroring `quiz.functions.ts`'s exact toast-wrapper shape. No new `useApi.ts` hook needed — `useUpdateData` (already wraps `apiPut`) was reused for the `PUT /assignment/submit/...` call, since the spec's suggested "add a PUT wrapper if none exists" check found one already there. `npx tsc --noEmit` and `yarn lint` both clean, zero new issues in any new/touched file (confirmed 28 errors/17 warnings total, same or fewer than the established baseline). `yarn build` clean, `manage-assignment/[moduleId]` route generated. Full Playwright click-through against the real local stack (both dev servers, live Neon dev DB, real `instructor1`/`instructor2`/`user1` seed accounts): create (title + rich-text instructions with an embedded GitHub link + due date) → redirect → reopen pre-filled in edit mode → student's "Assignment" row appears after the module's videos → instructions render with the link clickable → submit a GitHub URL → reload → persists and is still editable → resubmit with free-text ("I will do it later", accepted with no URL-format validation) → instructor's grading table shows the clickable link and the free-text row → grade (`7`, feedback) → student sees locked state, score/feedback, disabled textarea, no submit/update button → instructor reopens → student unlocked again, score/feedback gone, can resubmit → delete → module's Assignment row disappears for the student and "Manage Assignment" reopens to a fresh create-mode form. Also verified: client-side score-range validation (`Save Grade` disabled for `11`/`-1`, enabled for `7`, before any network call); a non-owning instructor visiting `manage-assignment/:moduleId` sees the (unrestricted-by-design) form/instructions but gets the axios interceptor's generic 403 toast on an update attempt, not a silent success or a blank/broken UI.
  - **Found and fixed one real backend bug during this click-through** — see `lms_server` spec [`34-assignment-submissions-ownership-gate-deviation.md`](../../lms_server/context/specs/34-assignment-submissions-ownership-gate-deviation.md): `assignment.service.ts::getAssignmentSubmissions` had an ownership check that directly contradicted spec 31's own Design section (which explicitly says this endpoint, like `getAssignmentForManage`, should be unrestricted — any instructor/admin can view, matching Quiz's precedent). This surfaced in the frontend as `SubmissionsTable.tsx` getting stuck on "Loading submissions..." for a non-owning instructor. Fixed by removing the ownership check (kept the existence/`404` check); re-verified live that the non-owning instructor now gets a real `200` with the submission list, while the write endpoints (`grade`/`reopen`) correctly remain `403`-gated (regression check). Test data (the assignment created during this click-through) was soft-deleted afterward, leaving no test artifacts.
  - **Full-system E2E follow-on** (per explicit user request, after the module-level Assignment click-through above): a from-scratch course/module/video/assignment created via the real UI, a brand-new student signed up and purchased it through the real SSLCommerz sandbox, then watched the video, saved a video note, and submitted the assignment — all working end-to-end. Found (documented, **not** fixed, per this test phase's explicit instruction) one real pre-existing bug unrelated to Assignment itself: `lms_server` spec [`35-stuck-pending-enrollment-no-retry.md`](../../lms_server/context/specs/35-stuck-pending-enrollment-no-retry.md) — `CourseDetailTop.tsx`'s "✓ Course Enrolled" state (driven by `check-user-enrolled`) doesn't reflect actual payment completion, so an abandoned/incomplete checkout leaves a student permanently stuck seeing "enrolled" with no way to retry, while every real content page `403`s. All test data (course/module/video/assignment/submission/note/enrollment/payment/the throwaway student account) deleted afterward; no lasting changes to `lms_client`.
- Implemented [`29-module-quiz-ui.md`](specs/29-module-quiz-ui.md): Feature 2 of 3 from the notes/quiz/assignment plan (Notes shipped in spec 28; Assignment not yet designed), consuming `lms_server` spec `29-module-quiz.md`'s new `quiz` endpoints. **Student side:** `EnrollCourseDetail.tsx` gained `activeQuiz` state (`{moduleId, quizId} | null`) — when set, the main content area (normally the `MuxPlayer` slot) renders the new `QuizPanel.tsx` instead, and `VideoNotesPanel` is hidden (a quiz isn't a video). `ModuleShowData.tsx`'s per-module accordion gets one extra row after its videos, gated on the new `module.quiz: {id} | null` field (added to `TModule` and to the backend's `getUserEnrolledCourse` select) — clicking it calls a new `onSelectQuiz` prop; selecting any video calls a new `clearActiveQuiz` prop so the player reliably swaps back. `QuizPanel.tsx` fetches `/quiz/take/:courseId/:moduleId` and distinguishes question-mode vs. permanent-results-mode by checking for `"attemptId" in data` (no separate boolean needed — mirrors the backend's own response-shape distinction); Submit is disabled until every question has a selection, and results render green for `isCorrect`/red for `wasSelected && !isCorrect` per question, matching the design doc's described UX exactly. **Instructor side:** new `components/main/(Instructor)/ManageQuiz/` — `ManageQuiz.tsx` (branches create-vs-edit off `GET /quiz/manage/:moduleId`'s `null`-or-not response, `DeleteModal` reused for delete), `QuizForm.tsx` (`react-hook-form` + `zodResolver`, top-level `useFieldArray` for questions), `QuizQuestionFields.tsx` (a second, nested `useFieldArray` per question for its options — react-hook-form requires one call per nesting level, and this is the first place this codebase has needed a nested field array). New `app/dashboard/instructor/manage-quiz/[moduleId]/page.tsx` (thin wrapper, `moduleId` read via `useParams()` inside the component itself, matching `UpdateModule.tsx`'s existing convention) and a new "Manage Quiz" row action on `ManageModule.tsx` (Instructor), hidden once the course is published — same restriction already applied to "Update Module"/"Add New Video" there. New shared top-level `functions/quiz.functions.ts` (`submitQuizFunction`/`createQuizFunction`/`updateQuizFunction`/`deleteQuizFunction`) rather than a feature-scoped one, since it spans both the student and instructor surfaces — same precedent as `video.functions.ts`. `npx tsc --noEmit` and `yarn lint` both clean, zero issues in any new or touched file (confirmed via targeted grep against the full lint run). Full Playwright click-through against the real local stack (both dev servers pointed at the live Neon dev DB) — real instructor account, real student account, real submitted answers: quiz creation with 2 dynamically-added questions and radio-marked correct answers → redirected to Manage Modules → reopened Manage Quiz for the same module → came back in edit mode, fully pre-filled → student saw the new "Quiz" row, answered, submitted → correct score (`2/2` for the answers chosen) with matching red/green coloring → reloaded the page and reopened the same quiz → identical permanent results shown immediately, no blank form, no way to retake → deleted the quiz through the real UI (not curl) → the module's "Quiz" row disappeared for the student; a module that never had a quiz correctly showed no row at all throughout. **Two real backend bugs were found and fixed during this click-through, not just frontend issues** — see `lms_server`'s progress tracker / spec [`30-quiz-recreate-after-delete-crash.md`](../../lms_server/context/specs/30-quiz-recreate-after-delete-crash.md): recreating a quiz on a module whose previous quiz had been soft-deleted crashed with an unhandled `500` (a hard, not partial, DB unique constraint on `moduleId`), and the fix for that alone would have left old `QuizAttempt` rows permanently blocking students from ever taking the reactivated quiz's new content — both fixed in the same backend session, re-verified end-to-end afterward.
- Implemented [`28-video-notes-ui.md`](specs/28-video-notes-ui.md): pure-consumer frontend for `lms_server`'s new `video-note` endpoints (spec 28, backend side, completed in a prior session). `EnrollCourseDetail.tsx`'s `videoDataObj` state widened to carry `id` (was just `{title, videoUrl}`); `ModuleShowData.tsx::handleGetVideo` now adds `id: result?.data?.id` to the `videoPayload` it builds. New `type/VideoNote.type.ts` (`TVideoNote`, mirrors the backend's Prisma model shape). New top-level `functions/videoNote.functions.ts` (`saveVideoNoteFunction`/`deleteVideoNoteFunction`), following `functions/video.functions.ts`'s exact toast-wrapper shape. New `VideoNotesPanel.tsx` (sibling to `ModuleShowData.tsx`), mounted in `EnrollCourseDetail.tsx`'s `leftVideoSection` below the video player, gated on `videoDataObj` being set: `useFetchData<TVideoNote | null>` keyed per `courseId`/`videoId` for the note, an uncontrolled `Textarea` (`key={videoId}`, `defaultValue={note?.content ?? ""}` — remounts fresh per video, avoiding a `set-state-in-effect` pattern), a "Save Note" button (`useUpdateData`/PUT), and a "Delete" button shown only when a note exists (`useDeleteData`/DELETE). `tsc --noEmit`: clean. `yarn lint`: 28 errors/17 warnings, all pre-existing baseline (down slightly from the previously-recorded 31/17, no new issues in any touched/new file — confirmed via grep). `yarn build`: clean, all 31 routes generated. Backend round-trip curl-verified against a local dev server (port 5000) with a locally-signed test-only JWT for the real enrolled test user (`user1@gmail.com`, "Frontend Development Using React"): empty note → `data: null`; create → row returned; re-fetch → same row; edit → same `id`, updated `content`/`updatedAt`; delete → `isDeleted: true`, re-fetch → `data: null` again; save-after-delete → same `id` reactivated (`isDeleted: false`), confirming the backend's upsert-reactivation path. Test note cleaned up (soft-deleted) afterward, throwaway server-side scripts removed, dev server stopped. **Not done:** an actual browser click-through (opening the real page, clicking Save/Delete, confirming the empty state and per-video isolation visually) — no browser-automation tool was available in this session (only curl-level API verification), left for the user. An unrelated whitespace-only reformat of `context/specs/28-video-notes-ui.md` (same background-formatter artifact seen on the backend spec file in a prior session) was caught via `git diff` and reverted before commit.
- **[`24-update-course-instructor-preselect-bug.md`](specs/24-update-course-instructor-preselect-bug.md)** — user report: `/dashboard/admin/update-course/[courseId]` never pre-selected the course's assigned instructor(s). `UpdateCourse.tsx` fetched with `useFetchData<TCourseData>` (defaults `instructors` to `string[]`), but `admin-course-detail` returns populated instructor objects — `reset()` handed the `react-select` `[{id,name,...}]` instead of `[id]`, so `.includes(id)` could never match. Fixed by typing the fetch `TCourseData<TInstructor>` and mapping to `.map(i => i.id)` in `reset()`. Live-verified.
- Fixed a duplicate sort-icon bug across **8** table-column files (`ManageModuleColumns.tsx`/`EnrollmentColumns.tsx` (Admin), `ManageVideoColumns.tsx`/`AssignCourseDetailColmn.tsx`/`ManageCourseColumns.tsx`/`ManageModuleColumns.tsx` (Instructor), `CertificateTableColumn.tsx`/`EnrolledCourseColumn.tsx` (User)) — each manually rendered its own `Button`+`ArrowUpDown` inside `header`, a leftover pattern from before `TableContent.tsx` centralized an automatic sort chevron for every sortable column; stacking a second icon on top, and double-toggling sort state per click (both the column's own `onClick` and the table's click handler fired). Replaced with plain string headers everywhere, matching the already-correct convention (e.g. `CourseColumns.tsx`). Also fixed, on direct user request (not full specs, small direct asks): error-log timestamps switched to 12-hour AM/PM with seconds (`ErrorLogsPage.tsx`/`ErrorLogDetailModal.tsx`); the error-log detail modal's width (user fixed independently on-disk, see `sm:max-w-[80vw]`).
- **[`lms_server` spec 25](../../lms_server/context/specs/25-course-list-meta-envelope-shape.md)** — user reported `GET /course/all-courses` nesting `data`/`meta` inside the outer `data` key. Backend-side fix (see that spec); frontend follow-on here: `CoursePage.tsx`/`PopularCourse.tsx` updated from reading `.data.data` to `.data` (backend's `data` is now the bare array), `hooks/useApi.ts`'s `TgenericResponse` gained an optional `meta` field for future typed consumers. Live-verified — `/courses` and the home page's Popular Courses section render correctly against the new flat shape.
- **Real-user end-to-end smoke test** (sign-up → login → purchase → watch all videos → mark complete → download certificate → submit review), Playwright-driven against the real UI/API, not mocked — including the actual SSLCommerz sandbox payment gateway. New account `user1@gmail.com` ("Tanvir Uddin Alif") purchased the one published course ("Frontend Development Using React", 4 modules/17 videos) and completed the full flow successfully. Found and fixed 3 real bugs along the way (specs [`21`](specs/21-signup-registration-payload-format-bug.md), [`22`](specs/22-course-detail-enrollment-review-check-404.md), [`23`](specs/23-review-submit-missing-feedback.md) above — see their entries in the table); a 4th (`getUserEnrolledCourse` leaking a soft-deleted module) was fixed on the `lms_server` side, see that app's tracker. Also flagged, not fixed: several `authCheck(UserRole.user)` endpoints (`enroll-course`, `give-review`) trust a client-supplied `userId` in the request body rather than deriving it from the verified JWT — a systemic pattern across the backend, raised to the user rather than patched unilaterally mid-test.
- Added `metadata` (`title`/`description`, `robots: noindex` on private/dashboard routes) to all 40 `page.tsx` files across the app — most admin/instructor dashboard pages had none at all, and several public pages only had a `title`. `app/(main)/sign-up/page.tsx` was `"use client"` inline (blocking a `metadata` export), so its form was extracted into `components/main/SignUp/SignUpPage.tsx` (matching the existing `Login`/`LoginPage.tsx` split) with the page left as a thin server wrapper. Root `app/layout.tsx`'s placeholder description ("Generated by create next app") replaced with a real one. `yarn lint`: no new errors beyond 2 pre-existing `no-explicit-any` ones carried over verbatim from the relocated sign-up code.
- Implemented [`17-postgres-id-migration.md`](specs/17-postgres-id-migration.md): the frontend half of the backend's Postgres/Prisma migration (`lms_server/context/specs/19-postgres-prisma-migration.md`, already complete). Renamed `_id`→`id` in the 3 source-of-truth type files (`types/course.types.ts::TCourseData`, `types/user.types.ts::TInstructor`, `types/module.types.ts::TModuleData`) plus the dead (zero-consumer) Redux `authSlice.ts`/`authTypes.ts::IUserInformation` for consistency; the real JWT-decode path (`hooks/useGetUser.ts::DecodedToken`) already correctly used `userId`, never `_id`, so needed no change. `npx tsc --noEmit` surfaced only 6 direct compile errors off those 3 renamed types (all in components destructuring `TInstructor`/`TCourseData` directly) — far fewer than the 102-occurrence count, because most consumption goes through `useFetchData<any>`-typed hook calls, which don't type-check response shapes at all. Confirmed via fresh grep against the plan's prior-audit numbers (43 files, 102 occurrences) that nothing had drifted since the plan was written, then did a verified-safe bulk `sed -i 's/\b_id\b/id/g'` across all 43 files (pre-checked for `_id`-as-substring collisions like `foo_idx` — none existed) to catch every remaining declaration and usage the compiler couldn't, since many are inside `any`-typed or self-consistent-but-stale local `type/` files. Re-ran `tsc --noEmit` after — it caught one more real (non-dead) usage this way (`ModuleShowData.tsx`'s `result?.data?.module` — now `.moduleId` — feeding `setSelectedModuleId`), confirming the bulk-rename-then-recompile approach surfaces live bugs even where per-file "is this actually read" review might reasonably let something through.
  - **Found and fixed a second-order issue the original plan didn't anticipate**: several backend FK fields that were plain scalar Mongoose refs (`user`, `course`, `Payment`, `module`, `instructor`) got renamed to Prisma's `<relation>Id` camelCase convention (`userId`, `courseId`, `paymentId`, `moduleId`, `instructorId`) as an intentional, already-approved backend design decision (spec 19's Stage 1.2) — but the frontend types still declared the old bare names. Verified against live API responses (not just types) which of these were real: `TEnrollCourseDetail.user/.Payment`, `TUserEnrolledCourse.user`, and several dead/unread table-row types (`EnrolledCourseColumn.tsx`, `ModuleShowData.tsx::TVideoDetail`, `EnrollCourseDetail.tsx::TCompleteEnrollment`, `AssignCourseDetailColmn.tsx`/`ManageModuleColumns.tsx`(Instructor)/`CourseDetailColumns.tsx`(Admin)'s `TModule.instructor`/`.course`) all needed the same rename. Confirmed via grep that `AddModule.tsx`/`UpdateModule.tsx`'s form-payload `course: string` and `courseEnrollment.function.ts`'s `TenrollCourse{user,course}` were correctly left alone — those are outgoing request bodies, and the backend's request-side field names (`module.validation.ts`, `CourseEnrollment.service.ts::enrollInCourse`'s payload) were never renamed, only response shapes were.
  - **Found and fixed 2 real backend regressions this way** (both in `lms_server`, logged in full in that app's progress tracker): `course.service.ts::getSingleCoureData` dropped `updatedAt` from its Prisma `select` where the original Mongoose exclusion-style `.select(" -published -__v -createdAt ")` implicitly kept it — caused an actual "Invalid time value" client-side crash on `/courses/[id]`, caught live via Playwright, not just inferred; `errorLog.service.ts::getAllErrorLogs` returned Prisma's `user: {...}` relation key instead of reshaping it into `userId: {...}` the way `review.service.ts` correctly did, breaking the frontend's `TErrorLog.userId: {id,name,email}` contract (caught by static cross-referencing before it could cause a runtime issue, since nothing had exercised that specific field in the UI yet).
  - `yarn build`: clean, all 31 routes generated. `yarn lint`: 31 errors/17 warnings, exactly the established pre-existing baseline (matches spec 16's last-recorded count), zero new issues in any touched file.
  - **Live-verified via Playwright** against the actual migrated Postgres backend (local dev servers, `lms_server` on port 5055 since 5000 is occupied by an unrelated project): public `/courses` → `/courses/[id]` (course detail rendered correctly post-fix, no more crash); full login → `/my-courses` → enrolled course detail (real module/video data, 100% progress, "Course Completed" badge, all correctly keyed by `.id`); `/dashboard/user/course-certificates` (real certificate row with correct course/student names and IDs); admin `/dashboard/admin/manage-course`, `/error-logs` (real error rows including correct `userId`-nested user info post-fix), `/manage-reviews` (real review row, correct course/student names), and `/dashboard/admin/course-detail/[courseId]` (nested module/video-count table rendering correctly). One **pre-existing, unrelated** hydration warning noticed on every dashboard page (`Sidebar`'s active-link `<a>` vs `<div>` SSR mismatch) — confirmed unrelated to entity IDs/data shapes (React recovers and all pages still render correct data after it), not fixed, out of this spec's scope.
- Implemented [`16-frontend-folder-structure-migration.md`](specs/16-frontend-folder-structure-migration.md): reorganized the entire `components/` tree (and a handful of top-level dirs) to match a reference project's conventions, as a pure mechanical file-move — no component logic touched. `components/Dashboard/` → `components/dashboard/sidebar/` (nav chrome: `Sidebar.tsx`, `AdminLinks.tsx`, `InstructorLinks.tsx`, `UserLinks.tsx`) + `components/main/(Admin|Instructor|User)/<Feature>/` (every dashboard feature screen, each now owning its own `column/`/`schema/`/`type/`/`functions/` subfolders where applicable — e.g. `ManageCourse/column/CourseColumns.tsx`, `ManageCourse/schema/Course.schemas.ts`, `ManageCourse/functions/course.functions.ts`). `components/main/publicPage/` flattened directly into `components/main/` (`AboutUs/`, `Course/`, `CourseDetail/`, `FAQPage/`, `Home/`, `Instructor/`, `MyCourses/`); `components/main/login/` → `components/main/Login/`. `components/input/` merged into `components/shared/input/`; `components/shared/` reorganized by category (`breadcrumb/`, `buttons/`, `Modal/` gained `DeleteModal.tsx`, `table/` gained `TableLoading.tsx`). Top-level: `actions/` and `services/` removed, merged into `lib/` (`lib/cookiesAction.ts`, `lib/auth.service.ts`, `lib/auth.ts`, `lib/jwt.ts`); `utils/axiosInstance.ts`/`api.ts` moved to `lib/`; `utils/useDebounce.ts` moved to `hooks/`; new `providers/` folder holds `QueryProvider.tsx` (was `app/QueryProvider.tsx`) and `StoreProvider.tsx` (was `lib/redux/provider/StoreProvider.tsx`); top-level `schemas/` removed (both files had a single feature consumer, colocated); `types/review.types.ts`/`video.types.ts`/`errorLog.types.ts`/`stat.types.ts` colocated into their single-consuming feature's `type/` folder, while `course`/`user`/`auth`/`ai`/`globalTypes`/`module` types stayed global (confirmed multi-feature-group consumers, or — for `module.types.ts` — no importer found at all, left global rather than guessed at). A handful of dead-code candidates surfaced during the consumer-usage audit (`functions/review.function.ts`, `functions/user.function.ts`, `functions/courseEnrollment.function.ts`, `components/shared/schema/imageSchema.tsx`, `types/module.types.ts` — none had any importer anywhere in the codebase) were left in place, not deleted, per the spec's explicit scope. ~135 files moved via `git mv`; ~104 import paths fixed (84 `@/`-alias replacements via a scripted literal find-replace anchored to the closing quote to avoid prefix collisions, plus ~20 relative `./`/`../` imports fixed individually where a sibling moved into a new subfolder or the importing file itself relocated). `node_modules` wasn't installed in this environment — ran `yarn install` first. `yarn lint`: 31 errors/17 warnings, all confirmed pre-existing code-quality issues unrelated to file location (`no-explicit-any`, `react-hooks/set-state-in-effect`, `no-img-element`, unused-vars) — zero import-resolution errors. `yarn build`: compiled cleanly, all 31 routes generated, TypeScript passed. Updated `CLAUDE.md`, `context/architecture.md`, `context/ui-context.md`, `context/code-standards.md`, and `context/ai-workflow-rules.md` to the new paths in the same change. **Not done**: an actual browser click-through of all three dashboards — left for the user, since this environment has no way to log in as each role; `yarn build`'s full successful static generation of every route is strong but not equivalent evidence.
- Implemented [`09-admin-dashboard-charts.md`](specs/09-admin-dashboard-charts.md) and [`10-review-moderation-ui.md`](specs/10-review-moderation-ui.md) together, as the frontend half of a paired feature with `lms_server` specs `12-admin-analytics-api.md`/`13-review-moderation-api.md` (implemented in the same session — see that app's progress tracker for the backend details). Two ambiguities the specs flagged were resolved with the user before implementation: spec 10 noted no bulk "all reviews" listing endpoint existed anywhere — resolved by adding `GET /review/all` (admin-only) to the backend, a small user-approved extension of backend spec 13's scope, rather than building a course-picker workaround; and whether deleting a review should reset the enrollment's `isReviewed` flag — resolved to yes.
  - **Charts (spec 09):** `yarn add recharts`. New `types/stat.types.ts` (`TRevenuePoint`, `TEnrollmentPoint`, `TAdminStats`). `StatPage.tsx` now types its `useFetchData` call with `TAdminStats` instead of an inline shape, replaces the hardcoded `"78%"` `AdminStatCard` with the real `averageCompletion` field, and renders two new chart components below the existing stat grid. New `components/Dashboard/admin/Stat/RevenueChart.tsx` (line chart) and `EnrollmentChart.tsx` (bar chart) — `recharts` `ResponsiveContainer`, each with a "No data yet" empty state. Colors use `var(--chart-1)`/`var(--chart-2)` (the existing shadcn semantic chart tokens in `app/globals.css`, light+dark aware) rather than the spec's suggested `prime-*` brand-accent tokens, since the codebase already has purpose-built chart tokens for exactly this — a deliberate deviation from the spec text, not an oversight. New `ChartSkeleton.tsx` loading placeholder, matching `AdminStatCardSkeleton`'s pulsing-rectangle style.
  - **Review moderation (spec 10):** New `types/review.types.ts` (`TAdminReview`). New `components/Dashboard/admin/ManageReview/ManageReviewPage.tsx`, following `ErrorLogsPage.tsx`'s structure (closest/most recent precedent) — `useFetchData(["all-reviews"], "/review/all")`, columns for course/student/rating (reusing `UserReviewCard.tsx`'s star-rendering approach)/comment (truncated)/date/delete action, delete flow via the existing `DeleteModal.tsx` + `useDeleteData([["all-reviews"]])` against `DELETE /review/:id`. New thin route `app/dashboard/admin/manage-reviews/page.tsx`. New "Manage Reviews" entry in `AdminLinks.tsx` (`Star` icon), following the "Error Logs" entry's exact precedent. No `middleware.ts` change — protected per `ai-workflow-rules.md`, and it wouldn't match `/dashboard/admin/manage-reviews` anyway (the pre-existing, already-documented gap); protection comes from the backend's `authCheck(UserRole.admin)` plus the existing 401-redirect Axios interceptor.
  - `yarn lint`: no new errors/warnings in any new or changed file (all flagged items are pre-existing, in unrelated files). `yarn build` clean — `/dashboard/admin/manage-reviews` appears as a new static route.
  - Playwright-verified end-to-end against the live local dev server with a locally-signed test-only admin JWT (not real credentials): Statistics page shows the new "Manage Reviews" sidebar entry, real (non-hardcoded) `averageCompletion`, and both charts correctly rendering their empty states (this live dataset has zero payments/enrollments in the last 30 days); Manage Reviews page renders all six columns and the table's built-in "No Data Available" empty state (this live dataset currently has zero reviews). Confirmed a `user`-role token visiting `/dashboard/admin/manage-reviews` gets redirected to `/login` (via the `401` → Axios-interceptor-logout path, not edge middleware). Did not fabricate verification of the delete-row/chart-with-real-data cases since no real review/payment/enrollment data exists in this environment right now — left as a spot-check for the user once real data exists.
- Implemented [`15-error-logs-admin-page.md`](specs/15-error-logs-admin-page.md): new `types/errorLog.types.ts` (`TErrorLog`), `components/Dashboard/admin/ErrorLogs/ErrorLogsPage.tsx` (`useFetchData(["error-logs"], "/error-log")`, columns for time/status badge/method/path/message/user/view-action via `GenericTableComponent`), `components/Dashboard/admin/ErrorLogs/ErrorLogDetailModal.tsx` (wraps the previously-unused `BaseModal`, shows message/error sources/method/path/ip/userRole/stack trace), thin route `app/dashboard/admin/error-logs/page.tsx`, and a new "Error Logs" entry in `AdminLinks.tsx` (`AlertTriangle` icon). `yarn lint`/`yarn build` clean (no new errors/warnings). Verified end-to-end via Playwright against the live local dev server with a locally-signed test-only admin JWT (deleted after use): sidebar entry visible, list renders real rows from the `errorlogs` collection with correctly colored status badges, "View" opens the detail modal with the full stack trace; confirmed `GET /api/error-log` returns 401 for both no-token and non-admin requests. Frontend-only — no backend changes, purely consumes the already-implemented `lms_server` endpoints.
- Implemented [`08-course-catalog-filters.md`](specs/08-course-catalog-filters.md): added a sort `Select` (Newest/Price: Low to High/Price: High to Low/Top Rated → `createdAt_desc`/`price_asc`/`price_desc`/`rating_desc`) next to the search bar in `CoursePage.tsx`, and a new sibling component `PriceFilter.tsx` (min/max number inputs, mirrors `CategoryFilter.tsx`'s card style) mounted below `CategoryFilter` in the sidebar. Both use local `useState` (min/max debounced 400ms via the existing `useDebounce`), matching how `searchTerm`/`categoryType` already work — the dead Redux `filterSlice` was confirmed unused anywhere in the codebase and intentionally left untouched, per the spec. Extended the existing `useFetchData` call's query string and `queryKey` array with `sortBy`/`minPrice`/`maxPrice`. `yarn lint`/`yarn build` clean (no new errors). This spec is **frontend-only by its own design** — `course.service.ts::getAllCourses` has no `sortBy`/`minPrice`/`maxPrice` handling yet, so the new controls currently send params the backend ignores; verified via Playwright that all four filters (search+category+sort+price) combine correctly in the UI/query construction with no conflicts, but did not fabricate verification of actual result reordering/narrowing since that isn't real yet. Per user decision, wrote a backend implementation proposal instead of implementing it: `lms_server/context/specs/18-course-catalog-sort-price-backend.md` (status: 📝 Proposed — awaiting review, not implemented) — it also flags that `rating_desc` needs a bigger aggregation-pipeline restructuring than a simple `.sort()`, plus two pre-existing bugs in the same function worth a look (`searchTerm` regex-matches a nonexistent `detail` field instead of `description`; `meta.totalCourses` isn't filtered).

## Recent Activity (from `git log`, `dev/monir` branch)

- `c20de8de feat: migrate to next js` — the frontend was migrated to Next.js (the current App Router structure).
- `4c3b66a3 fix(dashboard): instructor, user dashboard api update` — dashboard API-consumption fixes across `AddCourse`, `ManageInstructorPage`, `ManageAssignCourse`, `UpdateModule`, `MyCourseCertificates`, `EnrolledCourseColumn`, `MyEnrolledCourses`, plus an `envConfig.ts` tweak. **Note:** this commit also accidentally included `lms_server/node_modules/*` — unrelated to frontend work, but explains why `lms_server/.gitignore` is being reworked locally.
- `5ff8ff20 style: update ui`
- `05bcf0db fix: instructor dashboard api fixes` — `InstructorLinks`, `CourseColumns`, `CourseDetailColumns`, `ManageAssignCourse`, `AddModule`, `ManageModule`, `ManageVideo`, `NavBar`.
- `38522b13 fix(courseDetail): fix review eligibility api endpoint` — `CourseDetailPage.tsx`.

## Known Gaps / Open Questions

- No automated test suite configured.
- `lib/auth.ts` (moved from `services/auth.ts` by spec 16) contains a dead, commented-out `registration` function — the real signup flow (`app/(main)/sign-up/page.tsx`) posts to `/auth/register` directly via `usePost`. Candidate for cleanup, but not removed automatically — confirm before deleting in case something still imports it.
- Two data-orchestration file-naming conventions coexist (`*.functions.ts` and `*.function.ts` under `functions/`) — not unified; new files should match whichever suffix the relevant domain already uses.

## Next Up

Open — driven by whatever feature/fix is requested next.

(End of file - total 67 lines)
