# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

Live / in production (`devmats.vercel.app`) — ongoing maintenance & feature iteration, not initial build-out. The app was recently migrated to Next.js (see Recent Activity).

## Spec Implementation Status

Tracks work items defined in `context/specs/`. Update the moment implementation starts or finishes on a spec.

| Spec                                                                                           | Status         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`01-my-courses-list-redesign.md`](specs/01-my-courses-list-redesign.md)                       | ✅ Complete    | `MyCourses.tsx`, `MyCourseCard.tsx`, `MyCourseCardSkeleton.tsx` redesigned to match `/courses`' grid/card pattern; lint + build clean; visual pass confirmed by the user.                                                                                                                                                                                                                                                                                                                                                                               |
| [`02-my-course-detail-redesign.md`](specs/02-my-course-detail-redesign.md)                     | ✅ Complete    | `EnrollCourseDetail.tsx`, `NoVideoPlaceholder.tsx`, `VideoLoadingSkeleton.tsx`, `ModuleShowData.tsx`, `EnrolledCourseDetailSkeleton.tsx` redesigned (header, progress bar, card surfaces, grid layout, restyled module status colors, persistent completed badge); lint + build clean; visual pass confirmed by the user.                                                                                                                                                                                                                               |
| [`03-certificate-pdf-design.md`](specs/03-certificate-pdf-design.md)                           | ✅ Complete    | `CertificateDownloadButton.tsx` redrawn as a fully vector jsPDF landscape layout (border, wordmark, title, centered name, course name/category, date, certificate ID, signature line) — no more `addImage("/certificate.jpg", ...)`, which was silently failing since that file never existed in `public/`. `CertificateTableColumn.tsx` now passes `category`/`completedOn`/`userId`/`courseId` in addition to the existing `userName`/`courseName`. Lint + build clean; manual browser verification (actually clicking "Download") left for the user. |
| [`04-ai-review-summary-ui.md`](specs/04-ai-review-summary-ui.md)                               | ✅ Complete    | Created `types/ai.types.ts`, `components/main/publicPage/courseDetail/AiReviewSummary.tsx`, updated `CourseDetailPage.tsx` to render AI summary above review list. Build passes.                                                                                                                                                                                                                                                                                                                                                                        |
| [`05-ai-course-advisor-ui.md`](specs/05-ai-course-advisor-ui.md)                               | ✅ Complete    | Created `components/main/publicPage/course/AiCourseAdvisor.tsx`, extended `types/ai.types.ts` with `TCourseAdvisorResponse`/`TCourseAdvisorRecommendation`, integrated into `CoursePage.tsx` above course grid. Build passes; lint clean in new files.                                                                                                                                                                                                                                                                                                  |
| [`06-ai-study-assistant-ui.md`](specs/06-ai-study-assistant-ui.md)                             | ✅ Complete    | Created `components/main/publicPage/MyCourses/EnrolledCourseDetail/AiStudyAssistant.tsx` (floating trigger + chat panel, local `messages` state, `usePost` to `POST /ai/study-assistant/:courseId`), extended `types/ai.types.ts` with `TChatMessage`/`TStudyAssistantResponse`, mounted `<AiStudyAssistant courseId={id} />` as a sibling in `EnrollCourseDetail.tsx`. Lint clean in new/changed files (pre-existing baseline errors untouched); build passes.                                                                                         |
| [`08-course-catalog-filters.md`](specs/08-course-catalog-filters.md)                           | ✅ Complete    | Sort `Select` + `PriceFilter.tsx` added to `CoursePage.tsx`, wired into the existing `useFetchData` query string/`queryKey` (local `useState`, not the dead `filterSlice`). Lint + build clean. Backend companion work has since landed — `lms_server/context/specs/18-course-catalog-sort-price-backend.md` rewrote `getAllCourses` as an aggregation pipeline — so all four "Verify When Done" checkboxes are now ticked for real (live-browser-verified sort reordering and price narrowing against real catalog data), no longer blocked.           |
| [`15-error-logs-admin-page.md`](specs/15-error-logs-admin-page.md)                             | ✅ Complete    | New `/dashboard/admin/error-logs` page (list + detail modal), following the exact `ManageInstructorPage.tsx` pattern, consuming `lms_server`'s admin-only `GET /api/error-log`/`GET /api/error-log/:id`. Lint + build clean; Playwright-verified against the live local backend (real error rows, status badges, detail modal with stack trace); 401 confirmed for no-token/non-admin access.                                                                                                                                                           |
| [`09-admin-dashboard-charts.md`](specs/09-admin-dashboard-charts.md)                           | ✅ Complete    | `recharts` added; `RevenueChart.tsx`/`EnrollmentChart.tsx`/`ChartSkeleton.tsx` added under `Stat/`, consuming the extended `lms_server` `admin-stats` response. `StatPage.tsx`'s hardcoded `"78%"` replaced with the real `averageCompletion` value. Lint + build clean; Playwright-verified real chart rendering including empty-state handling.                                                                                                                                                                                                       |
| [`10-review-moderation-ui.md`](specs/10-review-moderation-ui.md)                               | ✅ Complete    | New `/dashboard/admin/manage-reviews` page (`ManageReviewPage.tsx`), following the `ErrorLogsPage.tsx` pattern, consuming a new `lms_server` admin-only `GET /review/all` (added as a small, user-approved extension of backend spec `13-review-moderation-api.md`, which only covered delete) and `DELETE /review/:reviewId`. Lint + build clean; Playwright-verified end-to-end including non-admin redirect.                                                                                                                                         |
| [`16-frontend-folder-structure-migration.md`](specs/16-frontend-folder-structure-migration.md) | ✅ Complete | Reorganized `lms_client`'s folder structure to match a reference project's conventions: `components/Dashboard/` split into `components/dashboard/sidebar/` (chrome only) and `components/main/(Admin\|Instructor\|User)/<Feature>/` (all dashboard feature screens, colocated with their own `column/`/`schema/`/`type/`/`functions/` subfolders); `components/main/publicPage/` flattened directly into `components/main/`; `components/input/` merged into `components/shared/input/`; `services/`/`actions/`/top-level `schemas/` removed (merged into `lib/` or colocated per-feature); `utils/axiosInstance.ts`/`api.ts` moved to `lib/`; new `providers/` folder for `QueryProvider.tsx`/`StoreProvider.tsx`; single-feature `types/*.types.ts` files colocated, cross-feature ones (`course`, `user`, `auth`, `ai`, `globalTypes`, `module`) stayed global. ~135 files moved via `git mv`, ~104 import paths fixed (84 `@/`-alias + ~20 relative). Pure file-move + import-path-fix — no component logic changed. `yarn lint`/`yarn build` both clean (lint's 31 pre-existing errors are unrelated code-quality issues, not import breaks). `CLAUDE.md`, `architecture.md`, `ui-context.md`, `code-standards.md`, and `ai-workflow-rules.md` updated to the new paths. Manual browser click-through of all three dashboards left for the user — see spec 16.                   |

## Completed (already implemented)

- Public site: home, course catalog + detail, about-us, contact, faqs, instructors, login/sign-up, SSLCOMMERZ payment-outcome pages.
- Auth: login, registration, JWT-cookie-based session, role-gated routing via `middleware.ts`.
- Admin dashboard: course CRUD + publish, instructor CRUD, module management, enrollment management, admin home/stats.
- Instructor dashboard: assigned-course view, module CRUD, video CRUD.
- User dashboard: enrolled courses, certificates.
- Shared: profile page, change-password.
- Data layer: `hooks/useApi.ts` (TanStack Query) + `functions/*.functions.ts` orchestration pattern in active use across course/module/video/review/enrollment flows.
- Admin dashboard: real revenue/enrollment charts + real average-completion stat (`Stat/StatPage.tsx` + `RevenueChart.tsx`/`EnrollmentChart.tsx`, via `recharts`), review moderation (`ManageReview/ManageReviewPage.tsx`, list + delete).

## Recent Activity (this session)

- Implemented [`16-frontend-folder-structure-migration.md`](specs/16-frontend-folder-structure-migration.md): reorganized the entire `components/` tree (and a handful of top-level dirs) to match a reference project's conventions, as a pure mechanical file-move — no component logic touched. `components/Dashboard/` → `components/dashboard/sidebar/` (nav chrome: `Sidebar.tsx`, `AdminLinks.tsx`, `InstructorLinks.tsx`, `UserLinks.tsx`) + `components/main/(Admin|Instructor|User)/<Feature>/` (every dashboard feature screen, each now owning its own `column/`/`schema/`/`type/`/`functions/` subfolders where applicable — e.g. `ManageCourse/column/CourseColumns.tsx`, `ManageCourse/schema/Course.schemas.ts`, `ManageCourse/functions/course.functions.ts`). `components/main/publicPage/` flattened directly into `components/main/` (`AboutUs/`, `Course/`, `CourseDetail/`, `FAQPage/`, `Home/`, `Instructor/`, `MyCourses/`); `components/main/login/` → `components/main/Login/`. `components/input/` merged into `components/shared/input/`; `components/shared/` reorganized by category (`breadcrumb/`, `buttons/`, `Modal/` gained `DeleteModal.tsx`, `table/` gained `TableLoading.tsx`). Top-level: `actions/` and `services/` removed, merged into `lib/` (`lib/cookiesAction.ts`, `lib/auth.service.ts`, `lib/auth.ts`, `lib/jwt.ts`); `utils/axiosInstance.ts`/`api.ts` moved to `lib/`; `utils/useDebounce.ts` moved to `hooks/`; new `providers/` folder holds `QueryProvider.tsx` (was `app/QueryProvider.tsx`) and `StoreProvider.tsx` (was `lib/redux/provider/StoreProvider.tsx`); top-level `schemas/` removed (both files had a single feature consumer, colocated); `types/review.types.ts`/`video.types.ts`/`errorLog.types.ts`/`stat.types.ts` colocated into their single-consuming feature's `type/` folder, while `course`/`user`/`auth`/`ai`/`globalTypes`/`module` types stayed global (confirmed multi-feature-group consumers, or — for `module.types.ts` — no importer found at all, left global rather than guessed at). A handful of dead-code candidates surfaced during the consumer-usage audit (`functions/review.function.ts`, `functions/user.function.ts`, `functions/courseEnrollment.function.ts`, `components/shared/schema/imageSchema.tsx`, `types/module.types.ts` — none had any importer anywhere in the codebase) were left in place, not deleted, per the spec's explicit scope. ~135 files moved via `git mv`; ~104 import paths fixed (84 `@/`-alias replacements via a scripted literal find-replace anchored to the closing quote to avoid prefix collisions, plus ~20 relative `./`/`../` imports fixed individually where a sibling moved into a new subfolder or the importing file itself relocated). `node_modules` wasn't installed in this environment — ran `yarn install` first. `yarn lint`: 31 errors/17 warnings, all confirmed pre-existing code-quality issues unrelated to file location (`no-explicit-any`, `react-hooks/set-state-in-effect`, `no-img-element`, unused-vars) — zero import-resolution errors. `yarn build`: compiled cleanly, all 31 routes generated, TypeScript passed. Updated `CLAUDE.md`, `context/architecture.md`, `context/ui-context.md`, `context/code-standards.md`, and `context/ai-workflow-rules.md` to the new paths in the same change. **Not done**: an actual browser click-through of all three dashboards — left for the user, since this environment has no way to log in as each role; `yarn build`'s full successful static generation of every route is strong but not equivalent evidence.
- Implemented [`09-admin-dashboard-charts.md`](specs/09-admin-dashboard-charts.md) and [`10-review-moderation-ui.md`](specs/10-review-moderation-ui.md) together, as the frontend half of a paired feature with `lms_server` specs `12-admin-analytics-api.md`/`13-review-moderation-api.md` (implemented in the same session — see that app's progress tracker for the backend details). Two ambiguities the specs flagged were resolved with the user before implementation: spec 10 noted no bulk "all reviews" listing endpoint existed anywhere — resolved by adding `GET /review/all` (admin-only) to the backend, a small user-approved extension of backend spec 13's scope, rather than building a course-picker workaround; and whether deleting a review should reset the enrollment's `isReviewed` flag — resolved to yes.
  - **Charts (spec 09):** `yarn add recharts`. New `types/stat.types.ts` (`TRevenuePoint`, `TEnrollmentPoint`, `TAdminStats`). `StatPage.tsx` now types its `useFetchData` call with `TAdminStats` instead of an inline shape, replaces the hardcoded `"78%"` `AdminStatCard` with the real `averageCompletion` field, and renders two new chart components below the existing stat grid. New `components/Dashboard/admin/Stat/RevenueChart.tsx` (line chart) and `EnrollmentChart.tsx` (bar chart) — `recharts` `ResponsiveContainer`, each with a "No data yet" empty state. Colors use `var(--chart-1)`/`var(--chart-2)` (the existing shadcn semantic chart tokens in `app/globals.css`, light+dark aware) rather than the spec's suggested `prime-*` brand-accent tokens, since the codebase already has purpose-built chart tokens for exactly this — a deliberate deviation from the spec text, not an oversight. New `ChartSkeleton.tsx` loading placeholder, matching `AdminStatCardSkeleton`'s pulsing-rectangle style.
  - **Review moderation (spec 10):** New `types/review.types.ts` (`TAdminReview`). New `components/Dashboard/admin/ManageReview/ManageReviewPage.tsx`, following `ErrorLogsPage.tsx`'s structure (closest/most recent precedent) — `useFetchData(["all-reviews"], "/review/all")`, columns for course/student/rating (reusing `UserReviewCard.tsx`'s star-rendering approach)/comment (truncated)/date/delete action, delete flow via the existing `DeleteModal.tsx` + `useDeleteData([["all-reviews"]])` against `DELETE /review/:id`. New thin route `app/dashboard/admin/manage-reviews/page.tsx`. New "Manage Reviews" entry in `AdminLinks.tsx` (`Star` icon), following the "Error Logs" entry's exact precedent. No `middleware.ts` change — protected per `ai-workflow-rules.md`, and it wouldn't match `/dashboard/admin/manage-reviews` anyway (the pre-existing, already-documented gap); protection comes from the backend's `authCheck(UserRole.admin)` plus the existing 401-redirect Axios interceptor.
  - `yarn lint`: no new errors/warnings in any new or changed file (all flagged items are pre-existing, in unrelated files). `yarn build` clean — `/dashboard/admin/manage-reviews` appears as a new static route.
  - Playwright-verified end-to-end against the live local dev server with a locally-signed test-only admin JWT (not real credentials): Statistics page shows the new "Manage Reviews" sidebar entry, real (non-hardcoded) `averageCompletion`, and both charts correctly rendering their empty states (this live dataset has zero payments/enrollments in the last 30 days); Manage Reviews page renders all six columns and the table's built-in "No Data Available" empty state (this live dataset currently has zero reviews). Confirmed a `user`-role token visiting `/dashboard/admin/manage-reviews` gets redirected to `/login` (via the `401` → Axios-interceptor-logout path, not edge middleware). Did not fabricate verification of the delete-row/chart-with-real-data cases since no real review/payment/enrollment data exists in this environment right now — left as a spot-check for the user once real data exists.
- Implemented [`15-error-logs-admin-page.md`](specs/15-error-logs-admin-page.md): new `types/errorLog.types.ts` (`TErrorLog`), `components/Dashboard/admin/ErrorLogs/ErrorLogsPage.tsx` (`useFetchData(["error-logs"], "/error-log")`, columns for time/status badge/method/path/message/user/view-action via `GenericTableComponent`), `components/Dashboard/admin/ErrorLogs/ErrorLogDetailModal.tsx` (wraps the previously-unused `BaseModal`, shows message/error sources/method/path/ip/userRole/stack trace), thin route `app/dashboard/admin/error-logs/page.tsx`, and a new "Error Logs" entry in `AdminLinks.tsx` (`AlertTriangle` icon). `yarn lint`/`yarn build` clean (no new errors/warnings). Verified end-to-end via Playwright against the live local dev server with a locally-signed test-only admin JWT (deleted after use): sidebar entry visible, list renders real rows from the `errorlogs` collection with correctly colored status badges, "View" opens the detail modal with the full stack trace; confirmed `GET /api/error-log` returns 401 for both no-token and non-admin requests. Frontend-only — no backend changes, purely consumes the already-implemented `lms_server` endpoints.
- Implemented [`08-course-catalog-filters.md`](specs/08-course-catalog-filters.md): added a sort `Select` (Newest/Price: Low to High/Price: High to Low/Top Rated → `createdAt_desc`/`price_asc`/`price_desc`/`rating_desc`) next to the search bar in `CoursePage.tsx`, and a new sibling component `PriceFilter.tsx` (min/max number inputs, mirrors `CategoryFilter.tsx`'s card style) mounted below `CategoryFilter` in the sidebar. Both use local `useState` (min/max debounced 400ms via the existing `useDebounce`), matching how `searchTerm`/`categoryType` already work — the dead Redux `filterSlice` was confirmed unused anywhere in the codebase and intentionally left untouched, per the spec. Extended the existing `useFetchData` call's query string and `queryKey` array with `sortBy`/`minPrice`/`maxPrice`. `yarn lint`/`yarn build` clean (no new errors). This spec is **frontend-only by its own design** — `course.service.ts::getAllCourses` has no `sortBy`/`minPrice`/`maxPrice` handling yet, so the new controls currently send params the backend ignores; verified via Playwright that all four filters (search+category+sort+price) combine correctly in the UI/query construction with no conflicts, but did not fabricate verification of actual result reordering/narrowing since that isn't real yet. Per user decision, wrote a backend implementation proposal instead of implementing it: `lms_server/context/specs/18-course-catalog-sort-price-backend.md` (status: 📝 Proposed — awaiting review, not implemented) — it also flags that `rating_desc` needs a bigger aggregation-pipeline restructuring than a simple `.sort()`, plus two pre-existing bugs in the same function worth a look (`searchTerm` regex-matches a nonexistent `detail` field instead of `description`; `meta.totalCourses` isn't filtered).

## Recent Activity (from `git log`, `dev/monir` branch)

- `c20de8de feat: migrate to next js` — the frontend was migrated to Next.js (the current App Router structure).
- `4c3b66a3 fix(dashboard): instructor, user dashboard api update` — dashboard API-consumption fixes across `AddCourse`, `ManageInstructorPage`, `ManageAssignCourse`, `UpdateModule`, `MyCourseCertificates`, `EnrolledCourseColumn`, `MyEnrolledCourses`, plus an `envConfig.ts` tweak. **Note:** this commit also accidentally included `lms_server/node_modules/*` — unrelated to frontend work, but explains why `lms_server/.gitignore` is being reworked locally.
- `5ff8ff20 style: update ui`
- `05bcf0db fix: instructor dashboard api fixes` — `InstructorLinks`, `CourseColumns`, `CourseDetailColumns`, `ManageAssignCourse`, `AddModule`, `ManageModule`, `ManageVideo`, `NavBar`.
- `38522b13 fix(courseDetail): fix review eligibility api endpoint` — `CourseDetailPage.tsx`.

## Known Gaps / Open Questions

- No automated test suite configured.
- `lib/auth.ts` (moved from `services/auth.ts` by spec 16) contains a dead, commented-out `registration` function — the real signup flow (`app/(main)/sign-up/page.tsx`) posts to `/auth/register` directly via `usePost`. Candidate for cleanup, but not removed automatically — confirm before deleting in case something still imports it.
- Two data-orchestration file-naming conventions coexist (`*.functions.ts` and `*.function.ts` under `functions/`) — not unified; new files should match whichever suffix the relevant domain already uses.

## Next Up

Open — driven by whatever feature/fix is requested next.

(End of file - total 67 lines)
