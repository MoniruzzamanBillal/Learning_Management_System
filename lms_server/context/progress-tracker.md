# Progress Tracker

Update this file after every meaningful implementation change.

## Current Phase

Live / in production (`lms-server-topaz.vercel.app`, backing `devmats.vercel.app`) — ongoing maintenance & feature iteration, not initial build-out. **Caveat:** the backend's persistence layer was migrated from MongoDB/Mongoose to PostgreSQL/Prisma (see spec 19 below). `.env`'s `DATABASE_URL` is now a real Neon connection string (not local Postgres, superseding the note this caveat used to carry) and was live-verified working (see Prisma 7 upgrade entry below) — but whether the *deployed* Vercel instance has been redeployed with this `DATABASE_URL` and the matching env vars (`CRON_SECRET`, etc.) set in the Vercel dashboard is still unconfirmed from this local sandbox. Don't assume the deployed API is Postgres-backed without checking.

## Spec Implementation Status

Tracks work items defined in `context/specs/`. Update the moment implementation starts or finishes on a spec.

| Spec                                                                                                     | Status      |
| -------------------------------------------------------------------------------------------------------- | ----------- |
| [`01-fix-sequential-video-unlock-order.md`](specs/01-fix-sequential-video-unlock-order.md)               | ✅ Complete |
| [`02-ai-review-summarizer.md`](specs/02-ai-review-summarizer.md)                                         | ✅ Complete |
| [`03-ai-course-advisor.md`](specs/03-ai-course-advisor.md)                                               | ✅ Complete |
| [`04-ai-study-assistant.md`](specs/04-ai-study-assistant.md)                                             | ✅ Complete |
| [`05-security-authcheck-and-hardening.md`](specs/05-security-authcheck-and-hardening.md)                 | ✅ Complete |
| [`06-rate-limiting.md`](specs/06-rate-limiting.md)                                                       | ✅ Complete |
| [`18-course-catalog-sort-price-backend.md`](specs/18-course-catalog-sort-price-backend.md)               | ✅ Complete |
| [`09-observability-logging-and-error-tracking.md`](specs/09-observability-logging-and-error-tracking.md) | ✅ Complete |
| [`12-admin-analytics-api.md`](specs/12-admin-analytics-api.md)                                           | ✅ Complete |
| [`13-review-moderation-api.md`](specs/13-review-moderation-api.md)                                       | ✅ Complete |
| [`19-postgres-prisma-migration.md`](specs/19-postgres-prisma-migration.md)                               | ✅ Complete |
| [`lms_client/context/specs/18-update-profile-page.md`](../../lms_client/context/specs/18-update-profile-page.md) | ✅ Complete |

## Completed (already implemented, wired into `router/index.ts`)

- `auth` — register (user + admin-registers-instructor), login, update password.
- `user` — instructor listing, logged-in user lookup, update user, change password.
- `course` — CRUD, publish, admin/instructor/public listings and detail views, admin stats.
- `courseModule` — module CRUD scoped to a course.
- `VideoModule` — video CRUD scoped to a module, including multi-video upload test endpoints (`/add-video2`, `/add-video3`).
- `CourseEnrollment` — enroll, list a user's enrolled/finished courses, per-course progress %, mark-complete, gated module/video access via `ValidateCourseAccess`.
- `VideoProgress` — internal only (no router); used by `CourseEnrollment` to compute progress.
- `payment` / `SSL` — SSLCOMMERZ success/fail/cancel callbacks.
- `review` — add/update review, course review list + average, eligibility check.
- `ai` — `GET /ai/review-summary/:courseId`: cached AI-generated pros/cons digest of a course's reviews (falls back to a fixed "not enough reviews" message under 3 reviews, no LLM call burned). `POST /ai/course-advisor`: public endpoint that accepts a plain-English learning goal and returns 2-3 recommended published courses with one-line reasons, hallucination-guarded (server-side cross-check against the fetched course list). `POST /ai/study-assistant/:courseId`: enrolled+paid-only (`authCheck(UserRole.user)` + `ValidateCourseAccess`) stateless chat endpoint — client resends full `messages` history each turn, server builds a grounding system prompt from the course's real module/video outline and this user's real progress/per-video status, and is explicitly told to admit it has no access to video transcripts rather than fabricate answers about video content.
- `errorLog` — `GET /error-log` / `GET /error-log/:id`, both `authCheck(UserRole.admin)`, plus `GET /error-log/cleanup` (`verifyCronSecret`-gated, Vercel Cron only). No public write endpoints; rows are only created internally from `globalErrorHandler`, which persists every error it normalizes (all 4xx/5xx, not just unexpected bugs) before responding. 30-day retention is enforced by the daily cron hitting `/cleanup` (post-migration; originally a Mongo TTL index, which has no Postgres/Prisma equivalent — see spec 19).
- `course.service.ts::adminStatistics` (`GET /course/admin-stats`, `authCheck(UserRole.admin)`) — extended with `revenueOverTime`/`enrollmentsOverTime` (day-by-day aggregations over the last 30 days) and a real `averageCompletion` percentage computed across all active enrollments via `videoProgressModel`.
- `review` — now also `GET /all` (admin-only, all reviews across all courses, populated with course/student name) and `DELETE /:reviewId` (admin-only, soft delete via `isDeleted`, resets the underlying enrollment's `isReviewed` so the student can leave a new review).

## Recent Activity (this session)

- **Implemented the backend half of [`lms_client/context/specs/18-update-profile-page.md`](../../lms_client/context/specs/18-update-profile-page.md)** — small companion piece to the frontend's new update-profile page. `PATCH /user/update-user` (`user.route.ts`) previously ran with no server-side validation at all (`req.body` reached `prisma.user.update` unchecked). Added `updateUserValidationSchema` (`user.validation.ts`, `{ name: z.string().min(1) }` — the only field the JSON `data` body carries; the avatar arrives separately as `req.file`) and wired it in via `validateRequest`, positioned after the route's existing inline JSON-parse middleware and before `authCheck` (no change to `authCheck`'s role list or the multipart/JSON-parse ordering otherwise). Live-verified against a running local instance: an empty `name` now returns a real Zod-shaped `400`; a valid `name` with no auth token correctly falls through to `authCheck`'s `401` — confirming the new validation actually runs, in the right order, ahead of auth. `yarn build` clean.
- **Upgraded Prisma from the pinned 6.19.3 to Prisma 7 (7.9.1)**, moving off the schema-embedded `datasource.url` in favor of the new driver-adapter model, per explicit user request. Changes:
  - `prisma/schema.prisma` — `generator client` switched from `prisma-client-js` to `prisma-client` with `output = "../src/generated/prisma"`, `moduleFormat = "cjs"`, `runtime = "nodejs"` (deliberately stayed CommonJS — the alternative, ESM, would've meant `"type": "module"` + a `tsconfig.json` rewrite + revalidating every import across the whole backend, which is out of scope for a Prisma bump). Dropped `binaryTargets` (no per-platform Rust query-engine binary under the driver-adapter model) and the `datasource.url` line (moved to `prisma.config.ts`).
  - New `prisma.config.ts` at the `lms_server/` root (alongside `package.json`, per Prisma 7's requirement — not next to `schema.prisma`) — this is what the CLI (`migrate`, `studio`, `db`) reads for the connection string now, via `env("DATABASE_URL")`.
  - `src/app/util/prisma.ts` — now builds a `PrismaPg` adapter (`@prisma/adapter-pg`, new dependency, wraps `pg`, also new) from `config.database_url` and passes it as `new PrismaClient({ adapter })`, per the project's existing convention of reading env through `config/index.ts` rather than `process.env` directly.
  - All 16 files importing model types/the `Prisma` namespace from `"@prisma/client"` (one `*.interface.ts` or `*.service.ts` per module) repointed to the generated client's real location — `"../../../generated/prisma/client"` (`"../../generated/prisma/client"` from `util/prisma.ts`, one directory shallower).
  - `package.json` — `build` changed from `tsc` to `prisma generate && tsc` (the generated client is gitignored, so a fresh `tsc` alone would fail without it; `postinstall` already covered the `yarn install` case but not a bare `yarn build` rerun). Added `@prisma/adapter-pg`/`pg`/`@types/pg`.
  - `.gitignore` — added `/src/generated` (the generated client, regenerated on demand, not committed).
  - `eslint.config.mjs` — added `src/generated/` to the top-level `ignores` array so `yarn lint` doesn't lint machine-generated code.
  - Verified: `yarn build` (full `prisma generate && tsc`) clean; `yarn lint` unchanged at the established 5-error/6-warning baseline (`src/generated/` excluded, confirmed no new files were the source); `npx prisma migrate status` (reads via `prisma.config.ts`) connected to the live Neon DB and reported the schema up to date; booted the dev server (`PORT=5099 yarn dev`, since 5000 is occupied — see the known `bikelog_server` port conflict) and hit `GET /api/course/all-courses` live — `200`, real (empty) query result through the new `PrismaPg` adapter, confirming the full config → generate → adapter → live-DB chain works end-to-end. Not part of this change and left alone: whether the deployed Vercel instance has this same upgrade — that's a separate deploy/redeploy step for the user.
- **Two post-migration regressions found and fixed** while implementing the frontend's companion spec ([`lms_client/context/specs/17-postgres-id-migration.md`](../../lms_client/context/specs/17-postgres-id-migration.md)), which cross-referenced every frontend type against real live API responses:
  - `course.service.ts::getSingleCoureData` — the Prisma `select` omitted `updatedAt`, where the original Mongoose exclusion-style `.select(" -published -__v -createdAt ")` implicitly kept it (exclusion syntax keeps everything not listed). This caused an actual client-side crash ("Invalid time value") on the frontend's course detail page, caught live via Playwright, not just inferred from a diff. Fixed by adding `updatedAt: true` to the select.
  - `errorLog.service.ts::getAllErrorLogs` — returned the Prisma relation under its natural key `user: {...}` instead of reshaping it into `userId: {...}`, the way `review.service.ts::getAllReviewsForAdmin` already correctly did. The original Mongoose `.populate("userId", "name email")` replaces the field *in place*, keeping the key name `userId` — this is the same reshaping pattern needed anywhere a populated relation's old key name doesn't match Prisma's relation field name. Fixed by mapping `{...rest, userId: user}` after the query, matching `review.service.ts`'s established pattern exactly.
  - Both `yarn build`/`yarn lint` reconfirmed clean after these fixes (same 5-error/6-warning baseline as before, no new issues).
- [`19-postgres-prisma-migration.md`](specs/19-postgres-prisma-migration.md) **Stage 2 complete — the whole migration is now done.** Modules 2.2–2.13 rewritten in dependency order (2.1/User covered separately below):
  - **2.2 Course** — heaviest rewrite, as the spec predicted. `getAllCourses` fully rewritten off the old Mongo aggregation pipeline: `rating_desc` sort fetches all matches and sorts/paginates in JS (Prisma can't `orderBy` a computed relation-aggregate directly), other sorts use real `orderBy`/`skip`/`take`; fixed the pre-existing paginated-vs-count filter mismatch by building both from one shared `where`. `adminStatistics`'s day-bucketed `revenueOverTime`/`enrollmentsOverTime` use `prisma.$queryRaw` with Postgres `date_trunc('day', ...)`, tagged-template only; `averageCompletion` uses two `groupBy`s (total + watched-only) merged and averaged in JS, matching the old Mongo `$group`/`$cond` aggregation. `Course.instructors`/`Course.modules` denormalized arrays dropped entirely per spec — instructors now go through the new `CourseInstructor` join model. Faithfully preserved (not "fixed"): `totalStudents`/`totalInstructors` still don't filter `isDeleted` — Mongoose's `countDocuments()` never triggered the `pre("find")` hook either, so this was never actually filtered even before.
  - **2.3 courseModule** — fixed the real `TModule.instructor` typing bug (was `ObjectId[]`, always used/enforced as singular at runtime) now that it's a proper required `instructorId` FK. Denormalized `Module.videos` array dropped (derived via `Video.moduleId`), which also **removed the need for the old Mongoose transaction** in `addModule` — creating a Module is now a single write, no secondary Course update to keep in sync.
  - **2.4 VideoModule** — `addVideo`'s `videoOrder` derivation carried forward as `prisma.video.aggregate({_max:{videoOrder}})`. Also rewrote `videoProgress.functions.ts::addVideoCoursePublish` here (formally a 2.8 item, but a hard dependency of `addVideo`) — takes a `Prisma.TransactionClient` instead of a Mongoose session, `createMany` instead of `insertMany`. Verified live: `videoOrder` came back `0` then `1` for two videos added in sequence (tested by calling `videoServices.addVideo` directly via a throwaway `ts-node` script, since real video upload needs a working Cloudinary + an actual video file neither available in this sandboxed verification).
  - **2.5 Payment / 2.6 CourseEnrollment** — done together since tightly coupled. `enrollInCourse`'s Mongoose transaction → `prisma.$transaction`; per decision #4, the SSLCommerz HTTP call stays inside the transaction unchanged. The old "update Payment with the new CourseEnrollment's id" step is **gone** — Payment has no physical FK back to CourseEnrollment in the new schema (`CourseEnrollment.paymentId` is the only physical FK; `Payment.enrollment` is Prisma's derived inverse accessor), so that follow-up write was simply unnecessary. `ValidateCourseAccess.ts` rewritten too (the exact `findFirst`-not-`findUnique` case the spec calls out by name). Verified live end-to-end: enroll → real SSLCommerz sandbox `GatewayPageURL` returned → simulated `/payment/success` webhook → `ValidateCourseAccess`-gated route passes → watched video 1 → video 2 auto-unlocked → progress hit 100% → `markCompleteCourse` succeeded.
  - **2.7 Review** — `addReview`'s new `@@unique([userId, courseId])` (decision #3) is caught via `Prisma.PrismaClientKnownRequestError`/`P2002` and rethrown as a friendly `AppError`, not a raw Prisma stack trace; verified live (posted a review, then a duplicate attempt got the friendly message, not a 500). Added an existence check to `updateReview` that wasn't there before — Prisma's `update()` throws on a missing row where Mongoose's `findByIdAndUpdate` silently returned `null`, and letting that throw uncaught would've been a new unhandled-error surface.
  - **2.8 VideoProgress** — no dedicated service/controller exists (both files are 0 bytes, as before); its only real logic is `addVideoCoursePublish` (done in 2.4) and the direct `prisma.videoProgress.*` calls inside `CourseEnrollment`/`VideoModule`/`ai`. Checked whether any of its call sites actually needed the upsert pattern decision #3 calls for — none do in practice, every create path is already guarded against duplicates by business logic upstream (e.g. `enrollInCourse` already rejects a second enrollment before ever reaching the `createMany`), so plain `create`/`createMany` throughout is correct as written.
  - **2.9 errorLog, ai, SSL, auth-glue** — `auth.model.ts` (confirmed 0 bytes) deleted. `SSL.service.ts` needed no changes (pure HTTP, no DB, as the spec predicted). `errorLog`: added `GET /error-log/cleanup` + `cleanupOldErrorLogs()` (`deleteMany` on `createdAt < 30 days ago`), a new `verifyCronSecret` middleware checking Vercel's auto-sent `Authorization: Bearer <CRON_SECRET>` header, `CRON_SECRET` added to `config/index.ts` and a freshly-generated random value appended to local `.env`, and a `crons` entry in `vercel.json` (`"0 3 * * *"`) — confirmed via web search that the legacy `builds`/`routes` format still supports `crons` alongside it, and that a once-daily schedule is fine even on Vercel's Hobby plan. `ai.service.ts` fully rewritten (course/module/videoProgress reads → Prisma); renamed the internal course-list prompt field from Mongo's `_id` to `id` (LLM-prompt-internal only, no external contract change). After this, deleted every remaining decoupled Mongoose "legacy shim" model file (`course.model.ts`, `module.model.ts`, `video.model.ts`, `VideoProgress.model.ts`, `CourseEnrollment.model.ts`, `user.model.ts`, `review.model.ts`, `payment.model.ts` — all had already had their last real consumer rewritten) plus the now-fully-obsolete one-off `src/scripts/fixVideoOrder.ts` backfill script (fixed a Mongo-only bug, moot after a from-scratch Postgres rebuild with no migrated data).
  - **2.10 validation ObjectId→UUID** — done inline as each module was touched (`course.validation.ts`, `module.validation.ts`, `videol.validation.ts`); final `grep -rl mongoose src/**/*.validation.ts` confirmed zero stragglers.
  - **2.11 DB connection bootstrap** — covered under Module 2.1 below (done first, as the spec required).
  - **2.12 soft-delete explicit-filter pass** — applied inline to every `User`/`Module`/`Video`/`Review` query as each service was rewritten; the `findFirst`-not-`findUnique` gotcha came up constantly (any lookup combining `id` with `isDeleted: false`) and was handled correctly throughout, including in `ValidateCourseAccess.ts`, the exact example the spec named.
  - **2.13 `.select()` exclusion-string conversions** — handled inline as encountered; all converted to explicit Prisma `select`/`include` shapes reproducing the original response shape (including a few "raw array of ids" fields like `Module.videos` where the old Mongo doc just held unpopulated `ObjectId[]`).
  - **Final state:** zero remaining `.model.ts` files in `src/app/modules/`; `grep -rl mongoose src` only turns up `globalErrorHandler.ts` (dead branches/comment), `handleCastError.ts`/`handleValidationError.ts` (now-dead Mongoose error-type normalizers, kept as out-of-scope — `mongoose` stays a `package.json` dependency solely so these compile), and the pre-existing (already unused before this migration) `app/builder/Queryuilder.ts`. None of these execute against a live DB anymore. `yarn build` clean; `yarn lint` **11 problems (5 errors, 6 warnings) — down from the established 14-error/13-warning baseline**, and every remaining one is in a file this migration never touched.
  - **Documentation sync done** per the spec's own checklist: `context/architecture.md` (stack table, system boundaries, middleware, 4 new invariants — explicit soft-delete filtering, `findFirst`-vs-`findUnique`, the Video partial-index-lives-in-migration-SQL caveat, plus the dead-Mongoose-error-branches gap), `context/code-standards.md` (Prisma query conventions, transaction pattern, `$queryRaw` guidance, friendly-P2002-mapping pattern), `context/project-overview.md` (two stray MongoDB/Mongoose mentions), and this file. Root `CLAUDE.md` updated too (stack line, module file-split, `errorLog`'s TTL→cron replacement, `DATABASE_URL` reuse note). Checked the Postman collection for `_id` references needing an `id` rename — found none; its only two `_id`-prefixed strings are Postman's own `_postman_id`/`_exporter_id` metadata, not domain fields, so nothing there needed changing.
  - **Not done / explicitly out of scope, flagged for the user:** deploying this to the actual Neon+Vercel production environment (verified only against a local Postgres instance — see the Current Phase caveat above); the frontend `_id`→`id` sweep (separate, not-yet-started spec per `frontend-migration-plan.md`); cleaning up the now-dead Mongoose error-normalization branches in `globalErrorHandler.ts`/`handleCastError.ts`/`handleValidationError.ts` and removing the `mongoose` package dependency (both are one and the same follow-up — can't do the latter without the former); adding generic Prisma-error normalization to `globalErrorHandler.ts` (only handled per-call-site where it mattered, e.g. `review`'s `P2002`).
- [`19-postgres-prisma-migration.md`](specs/19-postgres-prisma-migration.md) Stage 1 complete: user provisioned a local Postgres database (not Neon — a local instance was used for this build-out; `DATABASE_URL` in `.env` was repurposed from the Mongo URI, `directUrl`/`DIRECT_URL` were dropped from the schema and setup in favor of a single-URL config, both fine since Neon's separate pooled/direct split isn't required for a single-instance local Postgres). `npx prisma migrate dev --name init` applied the full schema; the Video partial-unique-index SQL was hand-added to the generated migration and additionally applied directly against the live DB (editing the migration file alone doesn't retroactively apply it once a migration is marked applied) — confirmed live via `\d "Video"` showing `Video_moduleId_videoOrder_active_unique ... WHERE "isDeleted" = false`. All Stage 1 exit criteria met.
  - Stage 2, **Module 2.1 (User) done**, including the auth.service.ts hashing audit the spec calls out explicitly within 2.1's scope (even though "auth" is formally a 2.9 item): `src/app/util/prisma.ts` added (serverless-safe singleton, step 2.11); `src/server.ts` now connects only Prisma (`mongoose.connect` removed entirely — this is a big-bang cutover per the spec, not a dual-DB rollout, and `DATABASE_URL` can only be one connection string at a time). `user.interface.ts` gutted to re-export the Prisma `User`/`UserRole` types as `TUser`/`TUserRole` (`authCheck.ts`'s `TUserRole` import stays structurally compatible, unchanged). `user.model.ts` (Mongoose) is deliberately **not yet deleted** — 5 other not-yet-rewritten modules (`course`, `courseModule`, `VideoModule`, `review`, `CourseEnrollment`) still import it; decoupled it from `TUser` onto a local `TLegacyUser` type so it keeps compiling standalone, marked with a TODO for deletion once its last consumer (module 2.8) is rewritten. `user.service.ts` and `auth.service.ts` fully rewritten to `prisma.user.*`, with explicit `bcrypt.hash()` at every create/update path (no Mongoose `pre("save")` equivalent exists in Prisma) and explicit `isDeleted: false` filters using `findFirst` (not `findUnique`, which can't combine a unique-field lookup with extra `where` conditions — the exact gotcha the spec flags). `auth.controller.ts`'s `loginUser` fixed from a Mongoose-only `userData.toObject()` call (which doesn't exist on a Prisma result) to a plain object copy + `delete`. **Discovered pre-existing bug, not fixed (out of scope for this migration, flagged for the user):** `authServices.createInstructor` never sets `userRole: "instructor"` on the created user — it silently falls through to the schema default (`"user"`), in both the original Mongoose code and this faithful Prisma port. Reproduced live: an admin-created "instructor" account came back `"userRole":"user"`.
  - `yarn build` clean; `yarn lint` improved from the established 14-error/13-warning baseline to **12 errors/13 warnings** — both fewer errors are incidental fixes (added missing `eslint-disable-next-line @typescript-eslint/no-explicit-any` comments on two pre-existing unsuppressed `file: any` params in `user.service.ts::updateUser` and `auth.service.ts::createInstructor` that this rewrite necessarily touched anyway), no new errors/warnings introduced.
  - Live-verified against the local Postgres DB with real HTTP requests (dev server on port 5055 — port 5000 is occupied by an unrelated project, `bikelog_server`, left alone): register → real UUID `id`, bcrypt-hashed password (`$2b$...`), default `userRole: "user"`; login → JWT issuance, password correctly stripped from the response; `change-password` → old-password compare succeeds/fails correctly, re-hash confirmed by a follow-up login with the new password; `get-instructors`/`loggedIn-user` work; role-gated `register-instructor`/`update-password` return `401` with no token or a non-admin token (pre-existing `authCheck.ts` behavior — it returns `401` for role mismatches, not `403`, unrelated to this migration) and succeed with a locally-signed admin-role JWT (test-only, promoted a throwaway local test user's `userRole` directly via `prisma db execute`, matching this codebase's established test-credential pattern from earlier sessions). Test server stopped after verification; throwaway test users left in the local dev DB.
- Started Stage 1.1 (setup): `yarn add -D prisma && yarn add @prisma/client` initially resolved Prisma 7, which removed schema-level `url`/`directUrl` and the classic `@prisma/client` import in favor of a driver-adapter model (`prisma.config.ts` + `@prisma/adapter-pg`) — not what the spec assumes. Pinned both packages to `6.19.3` instead so the spec's literal setup steps stay executable. Authored the full `prisma/schema.prisma` (all 9 models + the `CourseInstructor` join model + 3 enums per spec Stage 1.2); one Prisma-syntax fix was needed beyond the spec's prose — `CourseEnrollment.paymentId` needed `@unique` for Prisma to accept `Payment.enrollment` as a singular (not array) derived relation, matching the spec's own one-Payment-to-one-enrollment description. Added `"postinstall": "prisma generate"` to `package.json` (Stage 1.1 step 7) and `binaryTargets = ["native", "rhel-openssl-3.0.x"]` to the generator block (step 8). Schema validates and `prisma generate` runs clean against the pinned CLI. **Blocked** on Stage 1.1 step 2 (the spec's own "manual prerequisite, not automatable"): a real Neon Postgres project/connection string is needed before `prisma migrate dev --name init` (step 5) and the Video partial-unique-index SQL hand-edit (step 6) can run — `.env`'s `DATABASE_URL` is still the live production MongoDB URI. Stage 2 (module-by-module rewrite) has not started and depends on Stage 1 completing against a real database.
- Implemented [`12-admin-analytics-api.md`](specs/12-admin-analytics-api.md) and [`13-review-moderation-api.md`](specs/13-review-moderation-api.md) together, as the backend half of a paired admin-analytics-charts + review-moderation feature (frontend halves: `lms_client` specs `09-admin-dashboard-charts.md`/`10-review-moderation-ui.md`). Two spec-flagged ambiguities were resolved with the user before implementation: (1) spec 13 didn't include a bulk "list all reviews" endpoint, which the frontend spec explicitly needs and flagged as unresolved — decided to add `GET /review/all` (admin-only), extending spec 13's scope slightly, mirroring the existing `GET /course/admin-all-courses`/`GET /user/get-instructors` "list all X" pattern; (2) spec 13's open question on whether deleting a review should reset the enrollment's `isReviewed` flag — decided **yes**, so a moderation delete doesn't permanently block a student from ever reviewing that course again.
  - `course.service.ts::adminStatistics` — extended (not a new route, per the spec's stated preference) with three new aggregations: `revenueOverTime`/`enrollmentsOverTime` (day-by-day over the last 30 days, `$dateToString`/`$group`/`$sort` on `paymentModel`/`courseEnrollmentModel`), and `averageCompletion` (aggregates `videoProgressModel` grouped by `{user, course}` computing a per-enrollment watched/total ratio with `$cond` to avoid the `NaN` division-by-zero the existing single-pair `courseProgressPercentage` has, then averages across all groups). Also fixed a bug found while rewriting this function: the existing 30-day `revenue` sum wasn't filtering `paymentStatus: "Completed"` (it summed Pending/Failed payments too) — added the same filter the new time-series aggregation needed anyway, for internal consistency.
  - `review.model.ts` — added `isDeleted: { type: Boolean, default: false }` to the schema (the `TReview` interface already declared it) plus `pre("find")`/`pre("findOne")` hooks filtering `isDeleted: false`, mirroring `user.model.ts`'s exact pattern.
  - `review.service.ts` — `getAverageReviewOfCourse`'s `.aggregate()` got an explicit `isDeleted: false` added to its `$match` (aggregations bypass Mongoose query hooks, so this needed a manual fix distinct from the auto-filtered `.find()` calls). Added `deleteReview(reviewId)` — fetches the review first to capture `userId`/`courseId`, then in a transaction (mirroring `addReview`'s existing session pattern) sets `isDeleted: true` and resets the matching enrollment's `isReviewed` to `false`. Added `getAllReviewsForAdmin()` — unpaginated `.find().populate(...).sort({createdAt: -1})`, matching the existing admin-listing convention (client-side table handles pagination).
  - `review.controller.ts`/`review.route.ts` — added `GET /all` and `DELETE /:reviewId`, both `authCheck(UserRole.admin)`.
  - `yarn build` clean. `yarn lint`: same 14 pre-existing baseline errors; warnings went from 12 (post-errorLog baseline) to 13 — the one new warning is a `no-console` in `deleteReview`'s catch block, an intentional match of the exact same pattern already used in `addReview`'s catch block, not a new issue.
  - Verified live against the local dev server with a locally-signed test-only admin JWT (not real credentials): `GET /course/admin-stats` returns the new fields with sane values (`revenueOverTime`/`enrollmentsOverTime` correctly empty — no payments/enrollments in the last 30 days in this dataset — `averageCompletion` a real computed number); `GET /review/all` returns `401` with no token and with a non-admin (`user`-role) token; `DELETE /review/:reviewId` returns `404` for a nonexistent ID and `401` with no token; confirmed `getAverageReviewOfCourse`/`getCourseReview` still work correctly with the new `isDeleted` filtering in place (no crashes, correct empty-array/undefined behavior). **Not verified end-to-end against a real soft-deleted review** — this live database currently has zero reviews, so the full delete → `isReviewed` reset → student-can-re-review flow couldn't be exercised with real data; the code path mirrors the already-proven `addReview` transaction pattern exactly, but this specific chain is worth a manual spot-check once real review data exists.
- Implemented [`09-observability-logging-and-error-tracking.md`](specs/09-observability-logging-and-error-tracking.md), rewritten in place from an earlier pino+Sentry proposal (never implemented — zero hits in `package.json`/`yarn.lock`) to a self-hosted MongoDB approach per explicit user decision (no external services, no new logging library). New module `src/app/modules/errorLog/` (`errorLog.interface.ts`, `errorLog.model.ts`, `errorLog.service.ts`, `errorLog.controller.ts`, `errorLog.route.ts`) — `GET /error-log` (latest 200, sorted desc, `userId` populated) and `GET /error-log/:id`, both `authCheck(UserRole.admin)`, no write endpoints exposed. `errorLog.model.ts` adds this codebase's first TTL index — `errorLogSchema.index({ createdAt: 1 }, { expireAfterSeconds: 60 * 60 * 24 * 30 })` — so rows auto-expire 30 days after creation with no cleanup job. Wired `errorLogServices.logError(...)` into `globalErrorHandler.ts`, called right before the JSON response is sent, using the handler's already-normalized `status`/`message`/`errorSources` locals (not re-deriving from the inconsistent `error.status`/`error.statusCode` naming across error types) plus `req.method`/`req.originalUrl`/`req.ip`/`req.user?.userId`/`req.user?.userRole`. `logError` wraps its `create()` in its own try/catch so a DB hiccup while logging can never crash the error handler or block the client's error response. Per explicit user decision, **all** errors are captured (4xx validation failures included, not just 5xx) — a complete request-failure history rather than a bug-only tracker. Registered `/error-log` in `router/index.ts`. `yarn build` clean; `yarn lint` shows the same 14 pre-existing baseline errors plus one new `no-console` warning (12 total, up from 11) in the catch-block `console.error`, matching the same pattern already present in several other services' catch blocks. Verified live against the running local dev server (auto-picked up via `ts-node-dev --respawn`): a deliberate bad-login (`POST /api/auth/login`, nonexistent email) and an unauthenticated `GET /api/error-log` call both got captured with correct `statusCode`/`method`/`path`/`message`; admin-only gating confirmed (`401` with no token, `200` with a locally-signed admin-role JWT used for testing only — no real credentials involved); TTL index confirmed present on the live `errorlogs` collection via `db.errorlogs.getIndexes()` showing `expireAfterSeconds: 2592000`. Per user direction, the `lms_client` admin-facing viewer page was **not** implemented this pass — written up instead as a proposal at `lms_client/context/specs/09-error-logs-admin-page.md` (📝 Proposed — awaiting review) for the user to review before requesting implementation.
- Implemented [`18-course-catalog-sort-price-backend.md`](specs/18-course-catalog-sort-price-backend.md): rewrote `course.service.ts::getAllCourses` from `find()` + post-fetch `Promise.all` into a Mongo aggregation pipeline (`$match` → `$lookup` reviews → `$addFields` averageRating/totalReviews → `$sort` → `$skip`/`$limit` → conditional `reviewData` fold via `$cond`/`$$REMOVE` → `$project` exclude scratch/hidden fields → `Model.populate()` for instructors). Added `sortBy` handling (`createdAt_desc`/`price_asc`/`price_desc`/`rating_desc` via a `courseSortMap` lookup) and `minPrice`/`maxPrice` as a `price: {$gte,$lte}` addition to the existing `params` filter object. Per explicit user decision, went with the full aggregation rewrite (not a cheaper interim) so `rating_desc` is a true global sort — sort now runs before pagination for every option, fixing the underlying bug for all four, and the aggregation also eliminates the prior N+1 per-course review query. Removed the now-unused `reviewServices` import. Left untouched, per spec's explicit scope: the pre-existing `searchTerm` regex bug (matches nonexistent `detail` field) and the unfiltered `meta.totalCourses`. `yarn build` passes; `yarn lint` shows the same 14 pre-existing errors/11 warnings baseline (two `course.service.ts` entries are pre-existing `any` usages in untouched functions, just shifted lines). Verified against the live local DB (4 real published courses, read-only): `price_asc`/`price_desc`/`createdAt_desc` all globally correct; paginated `price_desc` with `limit=2` across two pages proved sort-before-pagination works; `minPrice`/`maxPrice` narrow and clear correctly; combined with `category`/`sortBy` without conflicts; `rating_desc` doesn't crash and correctly omits `reviewData` for the (currently) review-less catalog, though a differentiated-rating spot-check is still owed once real reviews exist. Also browser-verified live against `lms_client`'s `/courses` page — visible reordering and price narrowing confirmed with no console errors — and closed the loop on `lms_client` spec `08-course-catalog-filters.md`'s two previously-blocked checkboxes, now ticked for real.
- Implemented [`06-rate-limiting.md`](specs/06-rate-limiting.md): added `express-rate-limit` (`yarn add express-rate-limit`) and a new `src/app/middleware/rateLimiter.ts` exporting `aiLimiter` (10 req/10 min per IP) and `loginLimiter` (10 req/15 min per IP), both with a shared `handler` returning `{ success: false, message, data: null }` to match `sendResponse`'s envelope shape instead of `express-rate-limit`'s default plain text. Wired `aiLimiter` as the first middleware on both `GET /ai/review-summary/:courseId` and `POST /ai/course-advisor` (`ai.route.ts`), and `loginLimiter` as the first middleware on `POST /auth/login` (`auth.route.ts`), ahead of `validateRequest` in each chain so no DB/LLM work happens before the count. No other routes touched — no global limiter, per the spec's explicit scope note. `yarn build` passes; `yarn lint` shows the same 14 pre-existing errors/11 warnings baseline, none new. Verified locally: 15 rapid requests to `course-advisor` (deliberately invalid body, so no real LLM call was made) returned 400 for requests 1-10 then 429 for 11-15 with the correct JSON shape; `review-summary` shares the same `aiLimiter` counter and returned 429 immediately once already tripped; 15 rapid wrong-credential `login` attempts returned 404 for 1-10 then 429 for 11-15. Middleware ordering confirmed by code inspection (limiter listed first in each affected route).
- Implemented [`05-security-authcheck-and-hardening.md`](specs/05-security-authcheck-and-hardening.md): re-enabled the three previously-commented-out `authCheck(...)` calls — `course.routes.ts` (`/admin-stats` → `authCheck(UserRole.admin)`), `module.routes.ts` (`/add-module` → `authCheck(UserRole.admin, UserRole.instructor)`), `video.routes.ts` (`/add-video` → `authCheck(UserRole.admin, UserRole.instructor)`), each matching the role set already used on sibling routes in the same file. Added `helmet` (`yarn add helmet`) and wired `app.use(helmet())` as the first middleware in `src/app.ts`, ahead of `cors`. The hardcoded-instructor-password item originally in this spec was descoped per an earlier explicit decision (see the spec file's Current State section) — `auth.service.ts::createInstructor` is untouched. `yarn build` passes; `yarn lint` shows the same 14 pre-existing errors/11 warnings as before, none new. Verified locally: all three routes return 401 with no token; `helmet` response headers (CSP, `X-Content-Type-Options`, `X-Frame-Options`, etc.) are present and CORS headers for `https://devmats.vercel.app` still work. The 403-with-wrong-role/200-with-valid-role checks need real admin/instructor credentials against the live production DB and were left for the user to run manually, per this session's same live-DB caution used on the AI-endpoint specs.
- Implemented [`02-ai-review-summarizer.md`](specs/02-ai-review-summarizer.md): scaffolded `src/app/modules/ai/` (`ai.interface.ts`, `ai.service.ts`, `ai.controller.ts`, `ai.route.ts`; no model/validation needed yet), registered `/ai` in `router/index.ts`, and added `aiReviewSummary`/`aiReviewSummaryReviewCount` cache fields to `Course` (`course.interface.ts`, `course.model.ts`). Reuses `reviewServices.getAverageReviewOfCourse`/`getCourseReview` and `askOpenRouter` — no new packages, no duplicated logic. `yarn build` and `yarn lint` are clean (lint has 14 pre-existing errors/11 warnings in unrelated files, none introduced by this change). **Not yet manually verified against a live courseId** — this environment's `DATABASE_URL` points at the live/production database and the LLM calls hit a real (rate-limited free-tier) API, so live-endpoint verification was left for the user to run rather than done automatically.
- Implemented [`03-ai-course-advisor.md`](specs/03-ai-course-advisor.md): added `ai.validation.ts` with Zod schema for the `query` body field; extended `ai.interface.ts` with `TCourseAdvisorRecommendation` and `TCourseAdvisorResponse` types; added `getCourseAdvice(query)` in `ai.service.ts` that fetches up to 50 published courses (`_id, name, description, category, price`), builds a system prompt instructing the model to choose ≤3 from the provided list and respond in strict JSON, calls `askOpenRouter` with `jsonMode: true`, parses with try/catch (returns empty array on parse failure), filters recommendations against the fetched course list (hallucination guard), and merges real `name`/`category`/`price` from the DB; added `getCourseAdvice` controller and `POST /course-advisor` route with validation. `yarn build` passes; `yarn lint` shows no new errors in AI module files (all pre-existing).
- Implemented [`04-ai-study-assistant.md`](specs/04-ai-study-assistant.md): added `studyAssistantSchema`/`chatMessageSchema` to `ai.validation.ts` (messages capped at 20 items / 2000 chars each — abuse guard, not a full rate limiter); added `TStudyAssistantRequest`/`TStudyAssistantResponse` to `ai.interface.ts`; added `getStudyAssistantReply(courseId, userId, messages)` in `ai.service.ts` — fetches the course (throws `AppError(BAD_REQUEST, ...)` if missing), builds an ordered module→video-titles outline via `moduleModel.find(...).populate("videos", "_id title videoOrder")` sorted per-module by `videoOrder`, gets overall progress via `courseEnrollmentService.courseProgressPercentage`, gets per-video watched/unlocked/locked status via `videoProgressModel.find({course, user}).populate("video", ...)`, serializes all of that into a system prompt (with an explicit instruction that it has no video transcript access and must say so honestly rather than invent content), prepends it to the client's message history, and calls `askOpenRouter`; added `getStudyAssistantReply` controller reading `req.params.courseId`/`req.user.userId`/`req.body.messages`; added `POST /ai/study-assistant/:courseId` route with `authCheck(UserRole.user)` → `ValidateCourseAccess` → `validateRequest` ordering (matches `CourseEnrollment.routes.ts`'s existing gated-route pattern). No new Mongoose model — chat history is stateless/client-held per the spec. No new packages. `yarn build` passes; `yarn lint` shows no new errors in AI module files (all pre-existing baseline). **Not manually verified against a live enrolled course** — same live-DB/live-LLM caveat as the other two AI endpoints, left for the user to run.
- Fixed sequential video-unlock bug — clicking video N sometimes unlocked video N+2/N+3 instead of N+1. Root cause: `VideoModule.addVideo` assigned `videoOrder` via `countDocuments`, which isn't covered by the `isDeleted:false` query hooks and isn't atomic, so deletes/re-adds/races could produce duplicate `videoOrder` values within a module; separately, `CourseEnrollment.getUserEnrolledModuleVideos` returned the video list unsorted and without `videoOrder`, so the UI's item order didn't match the real order anyway. See [`context/specs/01-fix-sequential-video-unlock-order.md`](specs/01-fix-sequential-video-unlock-order.md) for full details.
  - `video.service.ts` — `addVideo` now derives `videoOrder` from `max(existing videoOrder) + 1` instead of `countDocuments`.
  - `video.model.ts` — added a partial unique index on `{module, videoOrder}` (only over `isDeleted:false` docs) to reject duplicates at the DB level.
  - `CourseEnrollment.service.ts` — `getUserEnrolledModuleVideos` now selects `videoOrder` and sorts by it before returning.
  - `lms_client` `ModuleShowData.tsx` — `TModuleVideo`'s nested video type now includes `videoOrder`; the list is sorted by it before rendering (defense-in-depth on top of the backend sort).
  - Added `src/scripts/fixVideoOrder.ts`, a one-off backfill to repair `videoOrder` on modules already corrupted by the old logic — **not yet run against any database**; needs to be run manually (staging first) per the spec doc's Verify-when-done checklist.

## Recent Activity (from `git log`)

Backend-specific commits are sparse recently — most of the last several commits on `dev/monir` only touched `lms_client`:

- `60439a9c feat:course complete functionality` — last commit that actually changed backend code (a one-line fix in `CourseEnrollment.controller.ts`).
- `38522b13`, `05bcf0db`, `4c3b66a3`, `c20de8de` — all frontend-only (Next.js migration, dashboard API-consumption fixes, review-eligibility UI fix). Confirmed by `git show --stat` — no `lms_server/src` changes in these.
- Note: `4c3b66a3` accidentally committed `lms_server/node_modules/*` into git history — this is almost certainly why `lms_server/.gitignore` is mid-edit locally (adding `node_modules`, `.vercel`, etc. to ignore patterns). Not part of this context-doc work; flagging for awareness only.

## Known Gaps / Open Questions

- No automated test suite (`yarn test` is a stub).
- Hardcoded default instructor password (`auth.service.ts::createInstructor` sets `payload.password = "123456"`) — considered under `05-security-authcheck-and-hardening.md` and explicitly kept as-is per user decision; do not "fix" without asking first.
- `VideoModule` has leftover test endpoints (`/add-video2`, `/add-video3`) with no auth — worth confirming whether these are still needed before touching that module.

## Next Up

Open — driven by whatever feature/fix is requested next.

(End of file - total 63 lines)
