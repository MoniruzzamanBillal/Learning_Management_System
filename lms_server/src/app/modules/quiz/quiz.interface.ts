import {
  Quiz,
  QuizAttempt,
  QuizOption,
  QuizQuestion,
} from "../../../generated/prisma/client";

export type TQuiz = Quiz;
export type TQuizQuestion = QuizQuestion;
export type TQuizOption = QuizOption;
export type TQuizAttempt = QuizAttempt;
